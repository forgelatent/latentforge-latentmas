# LatentForge — incident_ledger.md

**Accumulated remediation history. Read at session start as part of the Trinity of Truth.**

This file is the canonical entry point for the April 18–20 benchmark contamination incident and subsequent remediation work. The April 18 root incident record is preserved at `docs/INCIDENT_2026-04-18.md` and remains the citation target for inline disclaimer headers across the codebase. The April 19 and April 20 supplements have been consolidated into this file and archived verbatim at `docs/INCIDENT_ARCHIVE/`.

Last meaningful update: September 19, 2026, fourth session (research-sweep: two open items settled at source, July 12 fix `755a79c` cold re-read passed, 17 template digests on disk not 6, the digest's Competitive Watch section is static text; kalshi-pull empty-answer ticket fixed `d7c8d09`, and the feed found to be first-page-only since its first pull; enumeration clause ratified into the Reproducer requirement; Pattern H cold re-read still owed September 21 or later; WakeForJob still the next real work; see the final entry)
Maintained by: John McGuire (Founder Engine), with Systems Engine (Claude) and Divergent Thinking Engine (Grok)

---

## Archived sources

This ledger is the canonical record. Source documents are preserved in archive for audit-trail purposes only.

| Document | Location | Original date |
|---|---|---|
| April 18 root incident record | `docs/INCIDENT_2026-04-18.md` (in place — referenced by inline disclaimer headers across the codebase) | 2026-04-18 |
| April 19 manual pipeline simulation + remediation supplement | `docs/INCIDENT_ARCHIVE/INCIDENT_SUPPLEMENT_2026-04-19_ARCHIVED.md` | 2026-04-19 |
| April 20 audit + Apr 19 corrections supplement | `docs/INCIDENT_ARCHIVE/INCIDENT_SUPPLEMENT_2026-04-20_ARCHIVED.md` | 2026-04-20 |
| Quarantined seed file (root cause artifact) | `docs/INCIDENT_ARCHIVE/policy_markets_seed_DEPRECATED_DO_NOT_LOAD.json` | 2026-03-29 (authored), 2026-04-20 (quarantined) |

The April 18 document is in-place because 20+ inline disclaimer headers across the codebase cite it directly; moving it would invalidate those references. The supplements were moved because their only references were from the April 18 document and the prior version of this ledger.

---

## Incident summary

The April 18 contamination incident produced three rounds of remediation across three days. This table is a navigation index; details are in the sections that follow.

| Date | Event | Mechanism | Status | Source archive |
|---|---|---|---|---|
| 2026-04-18 | Benchmark contamination detected | Synthetic seed file (`policy_markets_seed.json`) had been read as live Polymarket data since March 29; "vs crowd" claims compounded across BRAIN.md, benchmark report, grant draft, outreach drafts | Closed (April 18 scope; April 19/20 supplements found additional contamination) | `docs/INCIDENT_2026-04-18.md` |
| 2026-04-19 | Manual pipeline simulation + remediation | Production-wrapper simulation of the launchd agent sequence caught one broken Python file, one poisoned shell env, two content failures (text_swarm matching, commercialization-agent Social Proof Loop); two narrative agents unloaded | Partially closed (infrastructure fixes complete; matching contract + agent rewrites open) | `docs/INCIDENT_ARCHIVE/INCIDENT_SUPPLEMENT_2026-04-19_ARCHIVED.md` |
| 2026-04-20 | Confabulation discovery + shadow_match unremediation | text_swarm price-extraction bug + matching logic broken; shadow_match.py never remediated in April 18 pass (still loading the seed file); intra-engine confabulation pattern named as separate failure mode from cross-engine Social Proof Loop | Partially closed (extraction fix + seed quarantine complete; matching contract + shadow_match rewrite + kalshi_selector decision open) | `docs/INCIDENT_ARCHIVE/INCIDENT_SUPPLEMENT_2026-04-20_ARCHIVED.md` |

The April 18 incident is the root record; April 19 and April 20 are remediation rounds that revealed additional layers of the same underlying contamination. No external claims were made during any phase of the incident — impact was internal only.

---

## Corrections to prior incident documents

- **April 20, 2026 — INCIDENT_SUPPLEMENT_2026-04-19 retraction (shadow_match.py status).** The April 19 supplement asserted: *"Shadow Match script verified pulling real prices (DeSantis 2.6%, Vance 39.1%, Bitcoin 28.5%, etc.)"* This claim was retracted on April 20 after `grep SEED_FILE experiments/benchmark/shadow_match.py` revealed the script still loads `policy_markets_seed.json` — the fictional seed file named as root cause in INCIDENT_2026-04-18. The cited prices came from the seed file, not the live API. The April 19 supplement also implied shadow_match was part of the active pipeline; it is not — shadow_match has never been scheduled in launchd. The `brainload_handoff` summary drafted April 20 PM repeated this misapprehension and is retracted along with the supplement claim. Both retractions accepted by all four engines on April 20 after reproducer evidence.

- **April 29, 2026 — INCIDENT_SUPPLEMENT_2026-04-19 framing incomplete (commercialization-agent Social Proof Loop).** Audit of `commercialization_agent.py` during `state_manifest.md` drafting revealed the Social Proof Loop has two compounding channels, not one as documented in INCIDENT_SUPPLEMENT_2026-04-19. The REVENUE_DIR channel (`load_latest` sort logic) is described in `state_manifest.md`'s commercialization-agent entry. The supplement's framing of the failure mechanism is incomplete; the unload remains correct, but a future reload requires structural fixes named in the manifest.

- **May 23, 2026 — INCIDENT_2026-04-18 remediation scope incomplete (text-swarm swarm logic replaced with random number generation).** The April 18 incident record (`docs/INCIDENT_2026-04-18.md`) and its supplements describe the contamination remediation as fixing the data pipeline — replacing seed-file reads with live Polymarket reads. Tier 1 git evidence surfaced May 23 shows the same remediation commit (`6457e02`, "CRITICAL: Fix text swarm data pipeline - now uses live Polymarket API," April 18, 6:32 PM Pacific, authored by John McGuire) also deleted the real Anthropic API swarm and replaced it with `random.uniform(35, 75)` per-persona random number generation. The commit deleted 186 lines including three full persona system prompts, the `call_claude()` function with real `claude-sonnet-4-6` API calls, `parse_probabilities()` for response extraction, `log_contrarian()` CSV writer, and the full `main()` orchestration. It added 106 lines including the random-number swarm and a broken matching layer that uses naive token overlap with no threshold. The commit message did not mention the swarm replacement. Per Founder recollection on May 23, the swarm-replacement work was engine-influenced ("whatever I did, I did because one of the engines told me to do that"); the Founder authored the commit (per git) but does not specifically recall the decision. The May 23 audit confirms no contemporaneous record of the swarm replacement exists in any project document — not in the commit message itself, not in BRAIN.md's April 19 end-of-day manifest, not in `INCIDENT_2026-04-18.md`'s Section 5 Remediation, and not in either April 19 or April 20 incident supplement. All five documents frame the work as a data-pipeline fix only; the swarm-logic change is invisible across the contemporaneous documentation chain. The April 19 supplement describes text-swarm's matching layer failure but does not describe the swarm-logic replacement, indicating the documenters at the time did not perceive the swarm change as a separate event worth recording. All claims about *which engine* prescribed the change, *what was said*, and *whether removal was the engine's proposal or the Founder's execution of something else* remain Tier 3 inference from Founder memory, uncorroborated. The remediation scope was therefore narrower than the response — it closed the data-source gap but opened a swarm-logic gap in the same commit, undocumented. The text-swarm script subsequently ran two more times against the new random-number logic (output files `text_swarm_2026-04-19.md` and `text_swarm_2026-04-20.md`); fake output was not detected at the time on either day. The April 19 output was committed via `6488a0a` ("quarantine: commercialization_thesis.md → _ARCHIVE_QUARANTINE.md; preserve research outputs; discard narrative agent outputs") with no quarantine markers or warning metadata on the random-output file — the file modification in that commit was an incidental script re-run picked up by `git add`, not a deliberate edit recognizing the output as compromised. *Reproducer:* `git show 6488a0a -- experiments/benchmark/text_swarm_2026-04-19.md` shows the diff as a normal-shape output update (random numbers changed, "Live Polymarket data from" header updated to April 19 file, no warnings added). The script has not been executed since April 20, 2026 — confirmed by the absence of any `text_swarm_*.md` output files after that date. Real swarm logic survives in `experiments/benchmark/calibration_tracker.py` lines 110-141 (function `swarm_estimate`, model `claude-sonnet-4-20250514`, three system prompts, three API calls per market) — but the implementations were never identical (different model identifier, different prompt length, different aggregation, different output format), making build_log.md Section 3.2's "parallel implementations" framing directionally true but imprecise. *Reproducer chain:* `git log -p -S "random.uniform" -- experiments/benchmark/03_text_swarm.py` shows commit `6457e02` as the introduction point. `grep -n "random.uniform" experiments/benchmark/03_text_swarm.py` returns line 99 (current location of the stub). `ls -la experiments/benchmark/text_swarm_*.md | awk '{print $9}'` shows daily outputs March 30 through April 20 only, no outputs after April 20. `grep -n "anthropic\|claude\|swarm_estimate" experiments/benchmark/calibration_tracker.py` confirms surviving swarm at lines 110, 122, 128, 133, 232. **Status:** Finding documented; remediation scope expanded in `state_manifest.md` text-swarm entry (Issue 1 matching + Issue 2 swarm + restoration prerequisites 1-3). Restoration design work deferred to subsequent sessions pending multi-engine review on three decision points (matching contract design, swarm restoration architecture, audit-trail design). No reload of text-swarm into launchd until all three are resolved.

- **May 23, 2026 — Session-context failure: Founder + Systems Engine misstated text-swarm execution timing in tonight's session briefing.** During the May 23 session that surfaced the text-swarm random-number swarm finding (preceding entry), the Systems Engine drafted a briefing for the other engines (Gemini, ChatGPT, Grok) stating that the Founder had been running `03_text_swarm.py` *"on a daily basis (when loaded)"* through the five-week window since the swarm replacement. This was inference, not Tier 1 verified fact. All three engines flagged the claim as unverified in their independent responses but continued to reason on top of it without grounding the claim against Tier 1 evidence. Tier 1 evidence (the file-listing reproducer below) subsequently showed that `03_text_swarm.py` produced output files for March 30 through April 20 only — 22 daily outputs total — with the random-number swarm active for exactly two of those days (April 19 and April 20). The script has not been executed since April 20. Correct framing: the random-number swarm produced 2 days of undetected fake output, then the component went dormant for 33 days. Incorrect framing (the one in tonight's briefing): the swarm produced 5 weeks of undetected fake output. The two framings have meaningfully different severity profiles, and the engines' subsequent analytical work (Gemini's "Structure as Illusion of Validity," ChatGPT's "5-week non-detection window," Grok's "5+ weeks of fake intelligence layer") was directionally correct on the failure shape but quantitatively wrong on the failure surface — because they did not pause to verify before continuing to build framings on the unverified inferred claim. *Reproducer:* `ls -la ~/Projects/latentforge-latentmas/experiments/benchmark/text_swarm_*.md | awk '{print $9}'` shows the actual output file dates. *Disciplines violated:* the Reproducer Requirement (claim was not accompanied by a reproducer when introduced into the briefing), the Context-Filling Machine warning (inference was not flagged as inference in the briefing language). *Pattern observed:* engines may flag inference as unverified while still building substantive analysis on it. The flag-without-pause shape is itself a contamination risk — the act of flagging produces a false sense of epistemic safety while compounding continues. Future briefings authored by the Systems Engine should ground each load-bearing quantitative claim in a reproducer before introducing the claim into multi-engine review, and engines should pause-to-verify rather than flag-and-proceed when key claims are inference. **Status:** Correction logged. No structural changes to Trinity protocols tonight (Pattern D awareness — drafting protocols under cognitive pressure of the session that surfaced the failure is itself the named failure mode). The Trinity-evolution question (whether to add a "pause-to-verify on load-bearing claims" rule to existing operational protocols) is queued for fresh-session review.

- **May 24, 2026 — shadow_match.py audited; structural invalidity across data, metric, narrative, and cost layers (Pro-Thesis Optimization Loop, candidate pattern).** Audit of `experiments/benchmark/shadow_match.py` (259 lines, HEAD `aad034e`) surfaced seven findings that together form a closed loop in which the script is structurally incapable of falsifying the project's core hypothesis. The script was already known to load the quarantined seed file (Section 7 Finding 3, April 20); the May 24 audit found that the seed-file dependency is not an isolated data-source bug but the first step in a four-layer architectural failure spanning the data layer (seed-file load), the prompt layer (seed values injected as priming context), the scoring layer ("winner" defined as maximum distance from the fictional anchor, with no resolved-outcome scoring anywhere in the file), and the narrative layer (three hardcoded `grant_line` strings producing favorable grant-application prose for every possible verdict branch). The cost-comparison layer is independently broken in a structurally consistent way: a hardcoded `0.003` and `0.003 * 3` produce a mathematically guaranteed 3.0x ratio reported as if measured. Two minor hygiene findings (dead-code `SWARM_FILE` declaration, relative `BENCHMARK_DIR` path) are recorded separately below; they are lower-severity and do not participate in the loop.

  **Founder characterization (May 24, eligible for canonical record per multi-engine convergence):** "This is not a localized bug or an emergency amputation like text_swarm.py. This is an instance of an unchecked Pro-Thesis Optimization Loop baked directly into the system's empirical instrumentation. The script was structurally incapable of falsifying the project's core hypothesis. Because the 'ground truth' crowd value was a hand-authored fiction (`policy_markets_seed.json`), and the winning condition was defined as maximizing distance from that fiction, the script mathematically guaranteed a result that could be spun into a pre-written, favorable grant narrative. This is the exact materialization of the Social Proof Loop at the code level: the system was built to manufacture the very validation it needed to survive."

  **Multi-engine review (cold; Founder framing held back from briefing per epistemic wall discipline):**
  - **Gemini** independently converged on the same shape, naming it "Confirmation Automation / Tautological Benchmark Design (Narrative-Locked Validation Loop)" and describing the loop's four mechanical steps: inject crowd consensus → measure success as shift from anchor → assume fixed cost advantage without measuring tokens → map every outcome to pre-written grant justification. Severity assessment: structurally deeper than the May 23 text-swarm random-number finding because the failure is soft and survives ordinary inspection (code runs, output looks normal, logic is hollow).
  - **ChatGPT** named two components of the same loop without consolidating: (a) the script "operationalizes 'being more different from consensus' as equivalent to 'being better' — that is not just a scoring bug, it inverts the epistemic meaning of the benchmark," and (b) "the verdict, commercialization framing, and narrative implication are deterministically emitted from mechanically weak or invalid measurements; the script automatically manufactures explanatory narrative from structurally compromised metrics." ChatGPT noted the explicit structural parallel to the April 18 commercialization-agent failure (Pattern A Social Proof Loop) at smaller scope (no nightly compounding, no thesis-file write-back).
  - **Grok** named a complementary lens — "Zombie test harness / Remediation Surface Preservation" — describing how the broken code survived rather than what made it broken. Post-April-18 cleanup focused on the launchd-scheduled pipeline and left manual-only scripts like shadow_match in a half-fixed state where they execute and produce plausible-looking output while carrying unaddressed pre-contamination assumptions. Severity assessment: moderately lower than text-swarm because real model calls still happen at runtime, but the invalid test surface is broader.

  **Convergence as evidence.** The Founder framing was written before the multi-engine briefing was sent, and was deliberately withheld from the briefing. Gemini, working independently, arrived at the same structural shape. ChatGPT independently noted the parallel to the April 18 commercialization-agent failure (the canonical Social Proof Loop instance). The convergence — three independent observers naming the same closed loop without seeing each other's framings — is the strongest available evidence that the pattern is structural rather than interpretive. Grok's "Remediation Surface Preservation" framing is complementary, not competing: Founder/Gemini/ChatGPT describe what makes the code broken; Grok describes how the broken code survived cleanup. Both descriptions are simultaneously true.

  **Architectural findings (1-5):**

  1. **Loads quarantined seed file.** Line 23: `SEED_FILE = BENCHMARK_DIR / "policy_markets_seed.json"`. Lines 86-90: existence check, then `markets = json.load(open(SEED_FILE))`. The seed file was `git mv`'d to `docs/INCIDENT_ARCHIVE/` on April 20 (commit `466ec98`), so under current state the existence check fails — but the structural dependency in the code is unchanged. Already documented in Section 7 Finding 3 (April 20); preserved here as the first step of the loop. *Reproducer:* `grep -n "SEED_FILE\|policy_markets_seed" experiments/benchmark/shadow_match.py`.

  2. **Seed-file crowd probabilities interpolated directly into the prompt string passed to every model call.** Lines 94-98 build `markets_text` by iterating over the seed-file markets and concatenating `Current crowd probability: {int(float(m.get('current_price',0))*100)}%` for each market. Lines 100-108 define `single_prompt` with `markets_text` embedded at the top of the prompt. The same `single_prompt` variable is reused verbatim across all four model calls: line 112 (single Shadow call) and line 139 (inside the swarm loop, where all three persona agents receive the same prompt with only the `system` parameter varying). The variable name "single_prompt" is misleading — it is the prompt every model receives, not a single model's prompt. The mechanical consequence is that every model is told the (fictional) crowd answer before producing its own estimate. *Reproducer:* `sed -n '94,108p' experiments/benchmark/shadow_match.py`; `grep -n "call_claude" experiments/benchmark/shadow_match.py`.

  3. **"Winner" defined as maximum distance from the fictional anchor, with no resolved-outcome scoring.** Lines 183-185: `single_dist = abs(single - crowd)`, `swarm_dist = abs(swarm - crowd)`. Lines 188-195: `if swarm_dist > single_dist: winner = "SWARM"`, with the reverse for "SINGLE" and equality for "TIE". The `abs()` makes the metric directionless — overestimating and underestimating the crowd score are treated identically as "divergence." Verified by negative evidence: `grep -n -E "outcome|resolved|resolution|brier" experiments/benchmark/shadow_match.py` returns one match, and it is a prompt-string instruction ("give your probability estimate for the YES outcome") on line 104, not scoring logic. The file contains no resolved-outcome scoring of any kind. This is the structurally most consequential finding: the script does not score against truth; it scores against a hand-authored fiction. *Reproducer:* `sed -n '183,195p' experiments/benchmark/shadow_match.py`; `grep -n -E "outcome|resolved|resolution|brier" experiments/benchmark/shadow_match.py`.

  4. **Hardcoded cost values produce a tautological 3x ratio reported as if measured.** Line 219: `single_cost_per_market = 0.003  # approx claude-sonnet per call`. Line 220: `swarm_cost_per_market = 0.003 * 3`. Line 222: `print(f"Cost ratio: swarm costs {swarm_cost_per_market/single_cost_per_market:.1f}x more per market")` — mathematically guaranteed to print `3.0`. The comment "# approx claude-sonnet per call" self-acknowledges the value is an approximation, not a measurement. No token counting, no API usage logging, no actual cost data flows through the script. The "3x" figure is then cited verbatim in the swarm-wins grant prose (Finding 5, Branch 1) as evidence of cost advantage vs o1. *Reproducer:* `sed -n '215,225p' experiments/benchmark/shadow_match.py`.

  5. **Hardcoded grant-application prose produces favorable framing on every verdict branch.** Lines 226, 229, 232 define three `grant_line` strings, one per verdict branch (swarm wins, single wins, tie). All three branches produce commercialization prose advancing the project's thesis:
     - Branch 1 (swarm wins): "Our 3-agent text swarm produces greater useful divergence from crowd consensus than a single superforecaster model on the same markets, at 3x lower per-call cost than o1. This demonstrates that multi-agent coordination — not raw model capability — is the source of the edge. Phase 2 will test whether latent communication amplifies this coordination advantage further."
     - Branch 2 (single wins): "Note: Single superforecaster model matched swarm divergence. Phase 2 latent test will determine whether latent communication unlocks coordination advantage beyond text-based methods."
     - Branch 3 (tie): "Swarm and single model produced equivalent divergence, suggesting the coordination advantage may manifest primarily in latent communication rather than text-based agent coordination."

     Even when the experiment's stated hypothesis fails (Branch 2), the script writes prose pivoting to the next chapter of the thesis. Even when the result is a wash (Branch 3), the script reframes the wash as evidence for latent communication. There is no verdict branch that produces an unfavorable narrative. Lines 252-253 write the `grant_line` to the output markdown file under a labeled `**Grant framing:**` section, persisting it to disk for downstream citation.

     **Self-documenting evidence.** The file docstring (lines 4 and 9) explicitly names two purposes consistent with the loop: line 4, "to prove coordination efficiency" (the verb "prove" rather than "test" is itself epistemically significant in a benchmark); line 9, "→ Strengthens the Rain grant narrative." The structural shape is not an artifact of contamination drift — it was built into the script at authorship. *Reproducer:* `sed -n '225,240p' experiments/benchmark/shadow_match.py`; `sed -n '1,12p' experiments/benchmark/shadow_match.py`.

  **Hygiene findings (6-7):** lower severity, not part of the loop, recorded for completeness.

  6. **Dead-code `SWARM_FILE` declaration.** Line 25: `SWARM_FILE = BENCHMARK_DIR / f"text_swarm_{TODAY}.md"`. Never referenced anywhere else in the file. Suggests a planned read of text-swarm output that was abandoned. *Reproducer:* `grep -n "SWARM_FILE" experiments/benchmark/shadow_match.py` returns the single declaration only.

  7. **Relative `BENCHMARK_DIR` path.** Line 22: `BENCHMARK_DIR = Path("experiments/benchmark")`. The launchd absolute-path rule (BRAIN.md April 5) does not apply because shadow_match is manual-only and not in launchd. The relative path is a workflow fragility — the script breaks silently when run from outside repo root. *Reproducer:* `sed -n '22p' experiments/benchmark/shadow_match.py`.

  **Pattern elevation deferred (candidate pattern marker).** Per the Section 8 discipline ("Whether to formalize this as a named pattern in Section 8 is deferred pending a third instance") and Pattern D guard (don't elevate canonical-record patterns under cognitive pressure of the discovery session — the audit and this entry are within the same 24-48 hour window), the pattern is recorded here as a **candidate pattern marker** named **"Pro-Thesis Optimization Loop"** with the alternate engine framings preserved. The April 18 commercialization-agent Social Proof Loop (canonical Pattern A) is the first instance of the broader shape at agent scope; the shadow_match findings are the first instance at code scope within a single script. Whether the two instances together constitute sufficient grounding for Section 8 elevation is a question for a fresh-context session after the calibration_tracker audit completes — that audit may surface a third instance, or may confirm shadow_match as architecturally distinct. Future sessions discovering a second instance of code-scope Pro-Thesis Optimization Loop should reference this marker and decide on elevation.

  **Line-number corrections vs the May 23-24 working notes:** Finding 2 swarm call site is at line 139 (not line 124 as recorded in the May 23 handover). Finding 4 variable assignments are at lines 219-220 with print statements at 221-222 (not all clustered at 221-224). Finding 5 `grant_line` strings are at lines 226, 229, 232 (not 226-236 as a block). The structural facts are unchanged; line numbers in this entry reflect direct verification against the May 24 source at HEAD `aad034e`.

  **Status:** Finding documented. Remediation scope expanded: shadow_match cannot be reloaded until (1) the live-data rewrite already named in `state_manifest.md` (replace seed-file load with polymarket-pull read) is complete, AND (2) the scoring layer is rewritten against resolved-outcome data rather than distance-from-anchor, AND (3) the cost-comparison layer is rewritten to use measured tokens rather than hardcoded constants, AND (4) the hardcoded `grant_line` output is removed entirely. All four are pre-conditions; addressing only the data-source bug (current `state_manifest.md` framing) would leave the loop intact. `state_manifest.md` shadow_match component entry to be updated accordingly in a follow-on edit. *Cross-reference:* Section 7 Finding 3 (April 20 seed-file load discovery, the entry this audit extends); Section 8 Pattern A (April 18 commercialization-agent Social Proof Loop, the first canonical instance of the broader shape at agent scope).

- **May 24, 2026 — calibration_tracker.py audited; VALID restored.** Audit of `experiments/benchmark/calibration_tracker.py` (304 lines, HEAD `c01f7ba`) surfaced seven findings establishing structural validity, followed by five operational verification checks confirming the script's runtime health. Multi-engine review (Gemini, ChatGPT, Grok) on a hallucination-resistant v2 briefing produced clean convergence on the audit's central conclusion: the script does not exhibit the failure shapes that affected shadow_match.py (Pro-Thesis Optimization Loop, Section 4 May 24 first entry) or text-swarm (random-number substitution, Section 4 May 23 entry). The previous `VALID: no` framing in `state_manifest.md` rested on two assumptions ("scoring against contaminated or fallback baselines" and "pending text-swarm matching fix") that are both demonstrably wrong against Tier 1 source. The component is now `VALID: yes` per the updated `state_manifest.md` entry.

  **Findings (seven):**
  1. *Live Polymarket data sourcing, no detected seed-file dependency.* `grep -n "polymarket\|policy_markets_seed" experiments/benchmark/calibration_tracker.py` returns Polymarket Gamma API URLs at lines 50 and 89, zero matches for `policy_markets_seed`. Scope: direct inspection only — indirect contamination via imports separately checked in Finding 8.
  2. *Real Brier scoring against resolved outcomes.* Lines 193-195 compute `swarm_brier = (swarm_prob - outcome) ** 2` and equivalents for crowd and naive baselines. `outcome` is fetched via `check_resolution()` at line 87, which makes a live API call. Structurally distinct from shadow_match's `abs(prediction - anchor)` divergence-from-fiction scoring.
  3. *Real Anthropic API calls, no random-number substitution.* `swarm_estimate` (lines 110-141) builds three real API payloads with three distinct persona prompts, POSTs to `api.anthropic.com/v1/messages`, parses responses, averages results. `grep -n "random"` returns zero matches.
  4. *Fully self-contained.* `grep -n "text_swarm\|shadow_match\|open(" experiments/benchmark/calibration_tracker.py` shows the script's `open()` calls (lines 147, 153, 159, 165) read/write only its own `PREDICTIONS_FILE` and `BRIER_LOG_FILE`; `urllib.request.urlopen` calls go only to Polymarket and Anthropic. `grep -n "data/polymarket"` returns zero matches — the script does not read polymarket-pull's stored files; it calls Polymarket directly.
  5. *Plain statistical output, no hardcoded narrative.* The `print_track` function (lines 260-289) and surrounding output (lines 291-297) emit only counts, average Brier scores, BSS with explicit negative-means-worse semantics, entropy, dispersion, divergence. No `grant_line`, no `marketing_line`, no `verdict` branches, no pre-written commercialization prose. Dual-track output (policy + full categories) for transparency.
  6. *Live and current.* `tail -25 experiments/benchmark/calibration/brier_running.json` shows the most recent log entry dated May 24, 2026 (today). The May 24 entry (Paxton primary win) shows the swarm at 0.5 vs crowd at 0.57 vs outcome 1, producing swarm Brier 0.25 (tied with naive) vs crowd Brier 0.18 (better) — the "honest measurement showing swarm losing" shape that a non-rigged system produces.
  7. *Forecasting-quality observation, not structural.* The swarm-default-to-0.5 behavior on Texas primary markets is a forecasting-quality finding the script correctly surfaces via Brier scoring. It is not a structural failure; it is the script doing its measurement job and showing the swarm is weak on certain market types.

  **Operational verification checks (five):** These were requested by the three engines during the v2 briefing review and run post-engine-review.
  - *Fallback-to-0.5 density:* `grep -c '"crowd_prob": 0.5' experiments/benchmark/calibration/brier_running.json` returns 1 — 1 of 27 entries (3.7%). Fallback is rare, not flattening aggregate Brier scores. Closes the central concern all three engines raised.
  - *Import surface:* `head -n 35 experiments/benchmark/calibration_tracker.py` shows 5 stdlib imports (`urllib.request`, `json`, `os`, `pathlib`, `datetime`). No internal modules, no helper utilities from other benchmark scripts. Closes Gemini's import-surface concern.
  - *Git history since contamination reset:* `git log --oneline --since="2026-04-18" experiments/benchmark/calibration_tracker.py` returns only two commits: `f548904` (April 19 syntax fix from incident_ledger Section 6 Finding 1) and `d86f0ff` (April 18 reset snapshot). No code-logic changes in 36 days. The audited baseline is stable.
  - *Agent error frequency:* `grep -c "Agent error:" experiments/benchmark/calibration/cron.log` returns 12 errors across 2,610 log lines. Errors are 401 auth (3 on one market), timeouts (8 scattered), and DNS resolution failures (2 — same WiFi-off pattern seen in the May 9 kalshi-pull audit). Per the script's structural design (`return None` from `swarm_estimate` if `probs` list is empty + `continue` on `None` swarm_prob in the prediction loop), full-failure cases are skipped entirely, not silently recorded as 0.5. Closes Grok's and Gemini's API parsing degradation concern.
  - *Polymarket data layer health:* Direct call to `gamma-api.polymarket.com/markets?active=true&closed=false&limit=3` returned three real markets with current_price values updated within the last hour. The polymarket-pull launchd job has produced 8 consecutive daily JSON files (May 17-24, 720-777 KB each); `tail -50 ~/Projects/data/polymarket/cron.log` shows clean `SUCCESS on attempt 1` lines preceded by real `Open markets: N` counts — no silent-success pattern of the kind found in kalshi-pull on May 9. Both Polymarket integration paths (calibration_tracker direct + polymarket-pull stored) are alive and honest.

  **Multi-engine review (cold; v2 briefing with structural anti-hallucination defenses):** All three engines independently confirmed: no evidence of (a) seed-file contamination, (b) divergence-from-anchor scoring, (c) random-number substitution, or (d) hardcoded narrative output. All three recommended the same gating condition for promotion (verify fallback-to-0.5 density) and named complementary additional concerns (Gemini: import surface + API parsing degradation; ChatGPT: semantic validity of market selection; Grok: silent fallback degradation + git history). Post-verification, all three engines' gating conditions are closed.

  **The v1 hallucination and v2 structural defenses (canonical record):** The May 24 multi-engine review on calibration_tracker had two phases. The first briefing (v1) used the same shape as the May 24 shadow_match briefing — narrative findings + reproducer commands sent to the engines for review. Gemini's response to v1 contained fabricated findings: it described a `marketing_line` variable, a 3-branch `verdict` structure ("SWARM OUTPERFORMS SINGLE MODEL" / "SINGLE MODEL OUTPERFORMS SWARM" / "METRIC TIE"), `single_tokens_used = 1450` and `swarm_tokens_used = 1450 * 3` constants, and matching grant-prose. None of this code exists in `calibration_tracker.py`. The fabricated content was structurally aligned with shadow_match's actual failure shape — `marketing_line` is a direct echo of shadow_match's `grant_line`; the 3-branch verdict structure is shadow_match's actual 3-branch structure; the hardcoded cost ratio is shadow_match's actual hardcoded cost ratio. Gemini transplanted shadow_match's failure shape onto calibration_tracker, including specific variable names and line-number citations, and provided reproducer commands that would have immediately falsified its own findings if anyone had run them. This was a Context-Filling Machine failure (`intent.md` Decision Rule for Design Changes): an engine generating plausible findings to fit an expected shape when the underlying data does not support them. The failure was caught when the Founder ran one of Gemini's own cited reproducers (`sed -n '210,245p' experiments/benchmark/calibration_tracker.py`) and observed that the output bore no resemblance to the code Gemini described. The detection mechanism worked because reproducers in the audit infrastructure are operator-runnable, not engine-runnable — the engine could not falsify its own claims because it does not have terminal access.

  The v2 briefing was constructed with four structural anti-hallucination defenses: (1) embedded verbatim terminal outputs alongside each finding, so engines reason about actual evidence rather than imagined evidence; (2) explicit framing that engines lack terminal access and are reviewing the operator's audit, not the source; (3) explicit supersession of v1 with a note that v1's findings about hardcoded narrative output did not match source; (4) a citation requirement in Question 1 asking engines to point at the specific embedded output line they were basing each finding on, making any new hallucination loud rather than silent. All three engines on v2 produced substantive, careful responses citing real evidence from the embedded outputs. None hallucinated. The v2 defenses worked as designed. *Reproducer:* v2 briefing draft preserved in session context (May 24, this date); engine responses preserved in session context. Formal archival of the briefing-and-responses-as-artifact is deferred to a fresh-context session.

  **What the hallucination event does and does not constitute:** It is a real failure of an engine review, caught and contained. It is *not* elevated to a named pattern in Section 8 tonight — per the Pattern D guard (do not generate canonical-record patterns under cognitive pressure of the discovery session), elevating any pattern about engine hallucinations during the same 24-48 hour window in which the hallucination occurred would itself be the failure shape the guard exists to defend against. Whether the hallucination event warrants pattern elevation, separate documentation as a standalone incident, or operational protocol change (e.g., requiring embedded terminal outputs in all multi-engine briefings) is a question for a fresh-context session. This entry preserves the event factually so the future session has the evidence base; it does not prescribe.

  **Corrections to prior framing:** The `state_manifest.md` calibration-tracker entry's "depends-on: text-swarm, polymarket-pull" line was incorrect at the file level. Finding 4 shows zero text-swarm dependency, and Finding 4 + the data-layer verification show calibration-tracker calls Polymarket directly rather than reading polymarket-pull's stored files. The corrected dependency line in the new manifest entry names Anthropic API and Polymarket Gamma API as the actual dependencies, with explicit negative-evidence callouts for text-swarm, shadow_match, polymarket-pull (the launchd job), commercialization-agent, and BRAIN.md.

  **Pattern observation (recorded, not elevated):** Two structurally similar audits in the same 24-hour window — shadow_match (Pro-Thesis Optimization Loop confirmed) and calibration_tracker (clean) — produced opposite verdicts on apparently-similar components. The differentiator is that calibration_tracker scores against resolved binary outcomes via standard Brier formulas while shadow_match scored against a fictional anchor via `abs()` distance. This is a single, narrow structural property — the presence or absence of resolved-outcome ground truth in the scoring layer — and it determined the verdict. Future audits of forecasting components should treat resolved-outcome scoring as the primary structural test.

  **Status:** VALID restored. `state_manifest.md` updated to move calibration-tracker from "Active, untrusted" to "Active and trusted" with corrected dependency declarations, the audit findings, and the five verification checks. *Cross-reference:* `state_manifest.md` calibration-tracker entry (May 24 update); `incident_ledger.md` Section 4 May 24 first entry (shadow_match audit, contrast case); Section 4 May 23 entries (text-swarm random-number replacement and session-context failure, the earlier audits in this remediation arc); Section 7 Finding 1 (April 19 calibration_tracker syntax fix, the most recent prior code change to this file).

- **July 11, 2026 — Gamma API silent closed-filter discovered during Mode 1 loader install; two-pass fetch fix applied same session (Founder-approved Pattern D override).** During the loader install session (per `LOADER_HANDOVER_2026-06-28`), the first repo-location `--run` returned 3 of 8 registry markets; the Q5.1 identity check correctly hard-failed (ERROR sidecar, exit 2). Live-API probes established the cause at Tier 1: the Gamma `/markets` endpoint silently applies `closed=false` by default EVEN when querying by explicit condition_ids, and `closed=true` is an exclusive filter returning only closed markets. The two responses partition the registry exactly (3 live + 5 closed = 8, zero overlap). There is no single-call way to retrieve a registry containing any resolved market. This joins the known Gamma API traps (outcomePrices-as-JSON-string, `active=true` on closed markets, unreliable `endDate`); shared trait — the API fails silently, never loudly. A consequence discovered in the same diagnosis: the loader's `cause_of_death: "closed"` classification path was unreachable code as originally written — a closed market could never appear in a default-query response for classification to see. *Reproducers:* `curl -s "https://gamma-api.polymarket.com/markets?condition_ids=<resolved-cid>"` returns `[]`; same URL + `&closed=true` returns the market. Commit `6476c03` message contains the full record.

  **Fix (Founder-approved 5-item design, implemented in-session under explicit Founder override of the Pattern D separation):** `fetch_all_markets()` two-pass fetch (bare -> live; `closed=true` -> closed), merge by conditionId with duplicate-cid hard-fail guard. Identity check unchanged; classification unchanged. Q4 "ONE bulk call" amended to two (API-forced); Q5.3 provenance amended to dual hashes (`api_response_sha256_live` / `_closed`). Applied as an 11-swap all-or-nothing patch; verified via ast.parse -> selfcheck (8 markets in merged response) -> live run (`RETIRED_PRESENT live=3 retired=5`, exit 1). Item 5 install-gate satisfied on genuine June 30 resolutions: all 5 markets `state=RETIRED`, `cause_of_death=closed`. File hash lineage: `010a579a` -> `8df5f433` (June 28) -> `f487be95` (of record). The override is noted factually: the in-session fix succeeded through all gates; the April 19 finding-real-prescription-wrong record (3 of 4 prescriptions superseded) is the counterweight; sample size on successful overrides of this shape is one. A cold fresh-session re-read of `fetch_all_markets` is queued as cheap insurance, not remediation.

  **Secondary finding, same session:** the manual-simulation rule caught a zero-byte launchd wrapper — a paste-echo accident had truncated `scripts/run_mode1_loader.sh` to 0 bytes after a successful initial write; the empty file ran as a silent no-op with exit 0. Rewritten and content-verified (`wc -l` = 43). Factual note, not a pattern elevation: this is the second paste-echo file-corruption instance in the repo (first: the empty `^C` file in repo root, dated May 23, 2026, deleted tonight). `bash -n` and success messages are insufficient verification for heredoc-written files; content checks (`wc -l`, hash) are the standard applied in this session's work.

  **Follow-up, July 11, 2026 (afternoon session) — cold re-read of `fetch_all_markets` completed; two-pass fix verified, no defects.** The queued fresh-session insurance re-read on the Pattern D override was executed against the file of record (hash `f487be95...` verified at read time). Five checks, all PASS: (1) merge guard hard-fails loud on duplicate conditionIds (cosmetic only: error message names fewer cases than the guard actually covers); (2) both passes fail loud through `fetch_markets_raw` — HTTP error, timeout, non-JSON, or non-list body all raise FetchError after retries, no partial-data path; (3) the `closed=true` pass retains the condition_ids filter (`build_bulk_url` lines 197-206); (4) no explicit `limit` param — at 8 markets any truncation fails loud via the Q5.1 identity check, acceptable as-is; (5) dual provenance hashes are computed from raw as-received bytes (lines 619-620). Two observations recorded, not defects: (a) **the Q5.1 identity check is the sole guard against an empty-but-valid 200 response** — the fetch layer treats `[]` as success; do not weaken `check_identity` on the assumption the fetch layer has its own guard; (b) **add an explicit `limit` parameter to `build_bulk_url` if the registry ever grows** — this rides with the open registry-v2 Founder decision. The Pattern D override insurance is cashed: sample size on successful overrides remains one, now verified rather than only gated. *Reproducer:* `shasum -a 256 experiments/benchmark/04_market_state_loader.py` returns `f487be95...`; functions at lines 197-206 (`build_bulk_url`), 207-265 (`fetch_markets_raw`), 266-294 (`fetch_all_markets`), 547-571 (`check_identity`).

- **July 12, 2026 — research-sweep silently reported SUCCESS on runs that performed no fetches; fixed same session (second instance of silent-success; first: kalshi-pull May 9).** Discovered during a routine runtime check of the rescued cron.log (itself recovered July 11 via the log-path fix). Tier 1 evidence: 5 of 19 recent scheduled runs (Jun 17, 18, 19, 23, 26) plus the Jul 11 manual run show start/SUCCESS stamps <=1s apart — physically impossible for real API sweeps; all six produced digests of exactly 1,711 bytes differing only by date, reading "No new results found." / "No recent activity." Mechanism (source-verified): bare `except: pass` in all three fetchers swallowed every exception; non-200 responses also silently dropped (no else-branch on status check). Empty results rendered as "nothing found"; exit 0; wrapper stamped `SUCCESS on attempt 1`; the wrapper's 3-attempt retry never engaged. Consequence: ~25-30% of recent digests were false "all quiet" reports — for a component whose purpose includes catching thesis-threatening signals, the worst failure shape it has. Digests from the five affected dates must not be treated as evidence of a quiet field.

  **Fix** (Founder-approved 3-item design, in-session under explicit approval — second successful same-session fix after July 11's; sample size two, still not precedent): FETCH_STATS attempts/failures tally in all three fetchers; total-failure exits 1 (engaging wrapper retry); partial-failure writes a WARNING banner into the digest and a failures tally into the cron.log summary. Applied as a 9-swap all-or-nothing patch (in two verified parts after a first single-paste attempt aborted safely on a self-inflicted quoting error — consistent with the July 11 afternoon standard: pastes >60 lines go in verified parts). Verified: ast.parse; happy path exit 0 with `fetch failures: 1/19` (partial-failure branch exercised live on first run); WiFi-off path refused to write and exited 1. Commit `755a79c`; file hash of record `ae81b844...`.

  **Pattern note:** second instance of the shape the May 9 entry deferred pattern-minting on. Same outcome (SUCCESS on no-work), different layer (per-fetch swallow vs. whole-script swallow). Per Pattern D discipline, the minting decision goes to a fresh session, which now has two instances to weigh.

  *Reproducers:* `grep -E "Attempt 1 of 3|SUCCESS" research/daily-digest/cron.log` shows the <=1s pairs on the five dates; `ls -la research/daily-digest/2026-06-1*.md` shows the 1,711-byte files; `shasum -a 256 experiments/week1/scripts/research_sweep.py` returns `ae81b844...`; `grep -c "except: pass" experiments/week1/scripts/research_sweep.py` returns 0 post-fix.

- **July 12, 2026 — Round 5 (registry v2) closed; Founder decisions recorded.** Cold three-engine review of the registry v2 briefing (`2b24918`) completed per Pattern D: briefing drafted July 11, responses collected July 12 morning, synthesis in a fresh afternoon session. Integrity: zero hallucinations across all three responses (v2-briefing defenses held, second consecutive round); ChatGPT and Grok independently re-derived the date-clustering claim from Section 4 market names, promoting it from Tier 3 to Tier 1-verified (4 of 5 retired slugs carry explicit June-30/end-of-June dates); the July 12 morning taint fragment (Gemini/China-GDP-runway) matched Gemini's actual filed argument — contained, no distortion. Positions: Gemini (b), Grok (b), ChatGPT (c); unanimous on triggering v2 now; disagreement only on v1's fate. **Founder decisions (July 12 afternoon):** (1) v2 triggered; (2) v1 archive-on-handover — loader continues reading v1 until v2 is locked and verified, then repoint and archive with note; (3) selection rules locked: 15 markets, max 3 resolving per calendar month, ≥5 open-ended or 6+ months, version trigger at <10 live (urgent <6), $10K floor with max 2 written exceptions, category mix documented at selection time. Selection delegated to a same-day fresh session under four gates (rules-first, fresh Tier 1 pool, loader limit fix pre-lock, candidate-then-confirm with cold re-read before promotion). See `founder_inputs/2026-07-12_v2_selection_session_handoff.md`.

- **Deferred-pattern marker (Pattern F candidate).** Three engines independently named a shared underlying pattern during the May 23 session: engine-prescribed-during-emergency-response failures. The three framings: Gemini's "Emergency Optimization Blindness" (engine-behavior lens — engines under crisis optimization aggressively amputate blocking complexity), ChatGPT's "Emergency remediation scope collapse" (process lens — valid urgent fix creates unlabelled functional downgrade outside stated scope), Grok's "Accountability diffusion" (attribution lens — git records Founder authorship, engine actually made the call, no protocol surfaces the gap). The three lenses are complementary, not competing — they describe one phenomenon (engine prescribes change during emergency → Founder executes → commit message frames change in terms of stated scope, hiding broader effect → no later review catches the buried change). The first documented instance is the May 23 finding above (commit `6457e02`). Per the Section 8 discipline ("Whether to formalize this as a named pattern in Section 8 is deferred pending a third instance"), this pattern is *not* elevated to Section 8 tonight. Reason: elevating a named pattern during the same session that surfaced its first instance would itself be Pattern D (finding-real, prescription-wrong) — generating a canonical-record pattern under cognitive pressure of the discovery is structurally the same failure shape the pattern is meant to defend against. Future sessions discovering a second or third instance of the same underlying shape should reference this deferred-pattern marker and decide whether to elevate to Section 8 as Pattern F. The three engine framings are preserved here as raw analytical context for that future elevation decision.

---

## Findings from script audit, April 29, 2026

These are findings that emerged during the script audit conducted as part of state_manifest.md sub-task 1 (Path A, documentation governance track). They are structural observations about the codebase that prior incident documents do not address; they are recorded here for traceability rather than as corrections to prior framing.

### April 29, 2026 audit findings

- **compression-researcher launchd-rule violation and context-blind production state.** Audit of `latent_compression_researcher.py` revealed two of three declared file dependencies (DIGEST_DIR at line 14, BRAIN_PATH at line 18) use relative paths in violation of the absolute-path rule established in BRAIN.md April 5. Under launchd execution, both reads return placeholder strings via guard clauses, so the agent has been generating daily research suggestions since April 4 without project-specific context. Details and reproducer in `state_manifest.md`'s compression-researcher entry.

- **BRAIN.md is a runtime input to multiple production agents.** Of five audited agent scripts, two (revenue-strategist, compression-researcher) declare a runtime dependency on `BRAIN.md` and parse specific sections (`## 1. THE THESIS`, `## 3. 90-DAY GOALS`, `## 4. WHAT WE ARE BUILDING`, `## 11. ARCHITECTURE DECISIONS`) into agent prompt context. revenue-strategist's read works under launchd; compression-researcher's is broken (see entry above). Where the read works, BRAIN.md's `[INVALIDATED 2026-04-18]` banner and the surrounding pre-reset content propagate into the agent's prompt context as runtime input. This means BRAIN.md is not solely a strategic reference document — it is a load-bearing input to the production pipeline. Implications: edits to BRAIN.md are edits to agent behavior; the `[INVALIDATED 2026-04-18]` banner surrounding pre-reset content is being read into agent prompts where the dependency works; rewriting BRAIN.md (mentioned in `state_manifest.md`'s sub-task 5 immediate tasks list) requires considering the runtime-input role, not just the human-reference role. Specific dependency citations in `state_manifest.md`'s revenue-strategist and compression-researcher entries.

---

### May 2, 2026 audit findings

- **research-sweep arXiv matching drifted from BRAIN.md Section 12 spec.** BRAIN.md Section 12 ("THE RESEARCH NERVOUS SYSTEM," lines 475-503) named a relevance threshold mechanism: arXiv hits crossing relevance >0.8 were to be promoted to "morning strategy session agenda BEFORE Claude Code work" with the founder reading the digest before opening a coding session. The implementation in `experiments/week1/scripts/research_sweep.py` never built relevance computation. Output is generated via broad token matching across 13 search terms (drifted from the 9 terms specified in BRAIN.md Section 12 — some original terms missing such as "Latent-DARM"; some new terms added such as "AVP latent," "agent vector protocol," "AI population forecasting"). The combination of broad token matching + no relevance threshold produces structured-looking false matches in the daily digest. Examples from `research/daily-digest/2026-05-02.md`: search term "agent vector protocol" matched "Covariant Locally Localized Gravity and vDVZ Continuity" and "Stop Holding Your Breath: CT-Informed Gaussian Splatting for Dynamic Bronchoscopy"; "hidden state communication" matched "Uniaxial strain-driven ferroelastic domain control in LaAlO3" (materials physics); "AI population forecasting" matched "Chemical Taxonomy of ω Centauri" (astrophysics paper about a star cluster). The arXiv IDs in the digest also appear temporally malformed (2604.NNNNN appearing in May 2026 results, when arXiv format would have rolled to 2605 on May 1) — suggesting a separate query-construction or date-handling issue beyond the matching layer. The two drift surfaces (relevance-threshold drift and search-term drift) are independent; the relevance-threshold gap is the more structurally consequential one because it would have served as a downstream filter even if search terms were over-broad. **Pattern note:** this is structurally the same failure as Section 7 Finding 2 (text-swarm matching) — broad token matching producing structured-looking false matches without an upstream relevance contract. Two instances now exist of the same matching-contract-design failure in the codebase. Whether to formalize this as a named pattern in Section 8 is deferred pending a third instance. **Lower downstream impact than text-swarm matching:** research-sweep output is for founder review only ("Founder reads before opening Claude Code" per BRAIN.md Section 12) — it does not feed downstream agents. The state_manifest.md research-sweep entry's `scope: research-only, no downstream impact` framing remains correct; this finding refines that framing with explicit articulation of the contamination surface (false positives, not absence of signal). **Remediation deferred.** Fix requires either (a) building the relevance >0.8 threshold mechanism per BRAIN.md Section 12 spec, or (b) explicit reconciliation of search terms against the BRAIN.md Section 12 list, or (c) both. Reproducer: `cat research/daily-digest/2026-05-02.md` shows the false matches in arXiv section; `grep -A2 "search.*terms\|SEARCH_TERMS" experiments/week1/scripts/research_sweep.py` shows the current 13-term list for comparison against BRAIN.md Section 12 lines 475-503.

---

### May 9, 2026 audit findings

- **kalshi-pull wrapper reports SUCCESS on fatal API errors.** Cron log inspection on May 9, 2026 (during data-pull gap diagnostic following the May 5 / May 6 missing-pulls observation from May 6) revealed a Pattern A-shaped silent-failure bug in `experiments/week1/scripts/kalshi_pull.py` and/or its launchd wrapper. On May 5 and May 6, the script encountered a fatal `urllib3.exceptions.NameResolutionError` (DNS resolution failed for `api.elections.kalshi.com` — environmental, not script-level, attributable to laptop-closed wake-with-no-network state) but the wrapper logged `SUCCESS on attempt 1` immediately after the error message. No data file was written for those days, no retry was attempted (despite "Attempt 1 of 3" framing implying retry-on-failure), and the launchctl exit status registered as `0` (clean exit). Compare polymarket-pull's behavior on the same days: same DNS failure, but polymarket-pull's wrapper correctly reported `FAILED on attempt 3 (exit code 1)` and `PERMANENT FAILURE after 3 attempts — manual rerun required` after exhausting retries. polymarket-pull is honest about failures; kalshi-pull is not. **Pattern note:** this is a Pattern B extension (scope narrowness in remediation passes — the post-April-18 sweep audited data-content correctness in pull outputs but did not audit error-handling in pull wrappers). It is also a Pattern A-shaped failure surface in its own right: a job claiming success when it failed is exactly the contamination shape the post-April-18 architecture was designed to prevent. Whether to mint a separate pattern letter is deferred pending discovery of a second instance of wrapper-level silent success in another component. **Downstream impact note:** kalshi-pull is currently `[LOADED: yes | VALID: yes]` per state_manifest.md, but VALID: yes is now downgraded by this finding. The launchctl exit status that supports the VALID: yes designation is unreliable when the wrapper masks errors; a fresh session reading state_manifest.md cannot trust kalshi-pull's `VALID: yes` until the wrapper is fixed and the validation gate is re-evaluated. **Resolved May 11, 2026.** Fix shipped in script `experiments/week1/scripts/kalshi_pull.py`: added `import sys` and `sys.exit(1)` in the `except Exception as e` block (lines 7 and 55). Root cause was script-level, not wrapper-level — the shared wrapper `scripts/run_with_key.sh` correctly handles retry/exit-code logic for both kalshi-pull and polymarket-pull; the difference was that kalshi_pull.py wrapped its API call in `try/except Exception as e` which swallowed exceptions and let the function return normally (exit code 0), whereas polymarket_pull.py has no such wrap and lets exceptions propagate (default exit code 1). After the fix, kalshi-pull now exits non-zero on any exception, which triggers the wrapper's standard 3-attempt retry with 5-min delays — structurally identical to polymarket-pull behavior. Verified May 11 via two tests: (1) happy-path manual run produced 1000 markets saved and exit code 0; (2) WiFi-off manual run produced the same `NameResolutionError` log line seen in May 5 / May 6 cron.log entries, but with exit code 1 instead of the previous exit code 0. *Reproducer:* `tail -100 ~/Projects/data/kalshi/cron.log` shows the May 5 and May 6 entries with the `Error fetching Kalshi data: HTTPSConnectionPool... NameResolutionError` immediately followed by `SUCCESS on attempt 1` on the same date stamp. Compare to `~/Projects/data/polymarket/cron.log` for the same days, which shows the correct `FAILED on attempt 3` / `PERMANENT FAILURE` reporting.

- **Follow-on finding: kalshi_pull.py treats empty-data API response as success.** Observed during May 11 fix-verification reading of `kalshi_pull.py`: the script's success path writes `markets` to disk and exits 0 regardless of whether `markets` is empty. If the Kalshi API ever returns `{"markets": []}` or a 200 with no data (transient API issue, rate limit producing soft-fail, schema change), the script writes an empty JSON file and reports success. This is a different failure mode from tonight's exception-swallowed bug — the API call succeeds in HTTP terms but produces no data — and is not addressed by tonight's fix. **Remediation deferred** as a separate Tier 3 follow-on ticket. Fix would add a post-fetch sanity check (e.g., `if not markets: sys.exit(2)` with an explicit exit code distinguishing empty-data from network failure, so cron.log diagnostics can tell them apart). *Update September 19, 2026: resolved, commit `d7c8d09`; the script-level cause was wider than this ticket records. See the September 19 fourth-session entry.*

---

## Findings from manual pipeline simulation, April 19, 2026

The April 19 manual pipeline simulation ran each of the 8 launchd-scheduled agents in production-wrapper sequence, catching failures that would otherwise have surfaced as staggered alerts the next morning. *The simulation was a Founder Engine override: all four engines (Systems, Divergent, Analysis × 2) had converged on Path 3 — stop, verify at 4:40 AM April 20 automated run. The override produced seven findings (three infrastructure repairs and four content failures). It is evidence-base for Founder Engine prerogative on operational tempo.*

### Infrastructure issues found and repaired April 19 night

1. **`calibration_tracker.py` had broken Python syntax.** April 18 contamination sweep prepended a markdown disclaimer to the top of the file as raw text, causing `SyntaxError: invalid character` on import. Would have silently failed at 5:30 AM April 20. — `[RESOLVED, Apr 19 night]`. Repaired via Python-comment-block replacement; verified `ast.parse` clean. Repo-wide AST scan confirmed zero other broken `.py` files.

2. **`~/.zshrc` had a poisoned `ANTHROPIC_API_KEY` export.** Line 8 read `export ANTHROPIC_API_KEY=source ~/.zprofile` — a malformed assignment that set the env var to the literal string `"source"` (6 chars). Launchd pipeline unaffected (uses Keychain via `run_with_key.sh`), but manual testing impossible until repair. — `[RESOLVED, Apr 19 night]`. Repaired to load directly from Keychain.

3. **Plaintext Kalshi API key in `~/.zshrc` line 4.** Flagged April 19, not repaired that night. — `[RESOLVED, Apr 29]`. Original Apr 19 framing was "filed for future Keychain migration." That framing was wrong: the script accesses public endpoints only and does not need authentication. Resolved Apr 29 by deletion rather than migration. Cross-reference: Section 10, Apr 19 Item 9 + Apr 29 Audit stored credentials entry.

### Content issues found and deferred April 19

These four findings represent failures in agent behavior — narrative agents producing misleading or unusable output even after the April 18 reset. Apr 19 mitigations addressed the immediate operational risk (unloading affected agents from launchd); the structural and matching-logic fixes remain open. Several entries below also illustrate a broader pattern: Apr 19's prescribed repair direction was, in three of four cases, later revised — finding-real, prescription-wrong.

- **A. Text swarm market_mapping failure.** `03_text_swarm.py`'s `load_live_markets()` queries Polymarket for 11 hardcoded benchmark questions; none matched any real Polymarket market via the search-term logic; all 11 fell back to 50.0%. All post-reset swarm predictions were logged against 50% fallback crowd baselines, making calibration tracker's skill scores meaningless. — `[OPEN, framing superseded]`. Apr 19 prescribed merging shadow_match's "battle-tested search-term map" into text_swarm. That repair direction was wrong: April 20 supplement retracted the assumption that shadow_match's search-term map worked at all (shadow_match was loading the seed file, not the live API). Current open work is contract-design-first per Apr 20's reframing. Cross-reference: Section 10, Apr 19 Item 2 + Apr 20 Item 1.

- **B. Revenue strategist template contamination.** Daily output continued to reference Rain Grant, prediction-market-research framing, "latent agents hold probability distributions" as the commercial story. Prompt template was unchanged post-reset. Mitigation: unloaded from launchd Apr 19 night (two-engine consensus — Gemini + Claude). — `[OPEN, framing superseded]`. Apr 19 prescribed prompt rewrite. That prescription was rejected on Apr 19 itself by the Structural-vs-prompt distinction (ChatGPT/Gemini): narrative agents synthesizing misleading claims is a structural protocol violation, not a prompt bug. Current open work is structural (BRAIN.md runtime-input dependency, template contamination at the structural layer). Cross-reference: Section 10, Apr 19 Item 5.

- **C. Commercialization agent actively regenerating Social Proof Loop.** The most significant finding of Apr 19 night. The agent was reading `calibration_tracker` output showing Brier 0.0173 for Full Track and BSS −2.93 vs crowd, cherry-picking the absolute Brier while ignoring the negative skill score, generating prose like "performance in adversarial liquid markets where professional forecasters...", and writing to `commercialization_thesis.md` — a file that compounded daily, with each run reading the prior day's thesis as input. This was the exact failure mode the April 18 reset was designed to prevent, regenerating itself from the prompt template. Mitigation: unloaded from launchd Apr 19 night (unanimous four-engine consensus). — `[OPEN, framing extended]`. Apr 19 framed the Social Proof Loop as a single compounding mechanism (the THESIS_FILE write-back). Apr 29 audit revealed a second compounding channel (the REVENUE_DIR `load_latest` sort logic) — the Social Proof Loop has two compounding mechanisms, not one. Both must be addressed before the agent is reloaded. Current open work is structural fix per `state_manifest.md` commercialization-agent entry. Cross-reference: Section 10, Apr 19 Item 5 + Section 4, Apr 29 correction.

- **D. Calibration metrics uninterpretable.** Pre-April 19 swarm predictions scored against fictional seed baselines. Post-April 19 predictions scored against 50% fallbacks (per Item A). Neither reading is meaningful until Item A's matching contract is defined and implemented. — `[OPEN]`. Same dependency state. Cross-reference: Section 10, Apr 19 Item 3.

---

## Findings from April 20 audit

The April 20 audit examined the code paths behind the agent behaviors flagged April 19. Where Apr 19's findings were behavior-level (agents producing wrong output despite the reset), Apr 20's findings were code-level (the specific lines and type-check failures that caused the wrong outputs). The progression is itself a finding about how contamination unfolds — behavioral symptoms surface first, then closer inspection reveals code-level mechanisms — and it sets up the cross-cutting failure mode analysis in Section 8.

A note on coverage: Finding 5 from the source supplement (the retraction of Apr 19's "Shadow Match script verified pulling real prices" claim) is captured in Section 4, "Corrections to prior incident documents," and is not repeated here.

1. **`text_swarm` price extraction bug.** `get_live_crowd_price()` read `m.get("current_price")` — a field that does not exist in the Polymarket gamma JSON. The fallback chain attempted to read `outcomePrices`, but `outcomePrices` is a JSON-encoded *string* (e.g. `'["0.749", "0.251"]'`), not a list. The `isinstance(..., list)` check failed silently. Execution fell through to a hardcoded `0.5`. Every market. Every run. The "live data" claim was true (the script loaded the live JSON); the "used live prices" claim was false. — `[RESOLVED, Apr 20 night]`. Commit `987a171`. New `_extract_price()` helper with fallback chain: `outcomePrices` (JSON-parsed if string) → `bestBid`/`bestAsk` midpoint → `lastTradePrice` → `0.5`. *Reproducer:* `python3 -c 'import json; d=json.load(open("/Users/latentforge/Projects/data/polymarket/2026-04-20.json")); m=d[0]; print(type(m.get("outcomePrices")).__name__, m.get("current_price"))'` prints `str None` — confirming the field type mismatch and the missing field that produced the silent fallback.

2. **`text_swarm` matching logic broken.** The matching loop took the first market whose question contained any word longer than 3 characters from the benchmark query. High-frequency tokens like `"2026"` and `"April"` caused every benchmark question to map to one of the first few markets in the JSON (Ethereum intraday contract, Fiorentina UEFA qualification), regardless of actual topic. The output looked structured rather than random precisely because every benchmark question collapsed onto one of two specific markets — producing a bimodal 9.5%/99.5% distribution where 9.5% = Fiorentina (market #2, contains "2026-27") and 99.5% = Ethereum intraday (market #1, contains "April 20"). — `[OPEN, deferred Apr 20 night]`. Apr 20's four-engine consensus was to defer matching-logic redesign rather than do design work while exhausted; the work is now scoped as contract-design-first per Apr 20's reframing. This finding is the source of Apr 19 Item 2's supersession: Apr 19 had prescribed merging shadow_match's "battle-tested" search-term map, but if matching can fail this badly with high-frequency token collisions, no matching code without an upfront contract would have been safe. Cross-reference: Section 10, Apr 20 Item 1 + Apr 19 Item 2.

3. **`shadow_match.py` never remediated in April 18 pass.** Line 23: `SEED_FILE = BENCHMARK_DIR / "policy_markets_seed.json"`. The Apr 18 remediation rewrote `03_text_swarm.py` to pull live Polymarket prices but left `shadow_match.py` untouched, still loading the fictional seed file. The seed file's `current_price` field had been authored by hand on March 29, which made the resulting "shadow match" output look authoritative. The mechanism behind the miss: the Apr 18 sweep's scope was narrower than the scope of the contamination — it found and rewrote the agent in the active launchd pipeline (text_swarm) but missed shadow_match because shadow_match wasn't scheduled in launchd and didn't surface during the production-pipeline review. — `[OPEN]`. Seed file quarantined to `docs/INCIDENT_ARCHIVE/policy_markets_seed_DEPRECATED_DO_NOT_LOAD.json` Apr 20 (commit `466ec98`); script attempting to load the old path now fails with `FileNotFoundError`. shadow_match rewrite to live-data source still pending. *Reproducer:* `grep SEED_FILE experiments/benchmark/shadow_match.py` returns the seed-file reference. Cross-reference: Section 10, Apr 20 Item 3 + Section 2 Archived sources entry for the quarantined seed file.

4. **`01_kalshi_selector.py` orphaned and misnamed.** Script at `experiments/benchmark/01_kalshi_selector.py` was named "kalshi selector" but actually read `policy_markets_seed.json` and wrote to `experiments/benchmark/markets_YYYY-MM-DD.md`. Had nothing to do with Kalshi. The real `kalshi-pull` launchd job ran a different script at `experiments/week1/scripts/kalshi_pull.py` which hit the Kalshi API correctly. Pedagogical takeaway: a script's name is not evidence of what it does; future audits should verify behavior, not infer from filename. This was the *second* unremediated seed-file loader caught April 20 (after shadow_match) — Apr 20 caught two seed-file loaders that the Apr 18 sweep had missed, which is the concrete form of Apr 20's "broader audit warranted" observation. — `[RESOLVED]`. Deleted (commit `452633d`); file absence verified Apr 30 morning. Cross-reference: Section 10, Apr 20 Item 5.

---

## Failure mode analysis

This section is the cross-cutting analytical record of how the April 18–20 contamination unfolded. Section 6 documented behavioral findings (agents producing wrong output despite the reset), Section 7 documented code-level mechanisms (the specific lines and type-check failures behind the wrong output). Section 8 names the patterns that span both. Its source spine is the Apr 20 supplement's "Failure mode recap" — preserved verbatim below as the keystone analytical artifact — extended with patterns that emerged from Apr 29 audit work and from reconciliation across Sections 4, 6, 7, 9, and 10. Section 8 deliberately does not introduce new named failure modes beyond Apr 20's two-mode taxonomy; new named modes belong in their canonical home documents.

### The 4-event progression

This is the fourth compounding-narrative event in 72 hours, as recorded April 20:

| # | Event | Agent | Mechanism |
|---|---|---|---|
| 1 | Apr 18 | benchmark pipeline | "45% Brier improvement" synthesized from seed data |
| 2 | Apr 19 PM | commercialization agent | Cherry-picked Brier into compounding thesis file |
| 3 | Apr 19 PM | INCIDENT_SUPPLEMENT authors | Asserted shadow_match pulling real prices without grep |
| 4 | Apr 20 AM | Grok | Generated "low-liquidity 50%" thesis without inspecting JSON |

### The common pattern

Apr 20's analysis of the four events identified a four-stage sequence: engine reads ambiguous output → generates confident narrative → forward-propagates without returning to raw data → narrative calcifies into artifact layer (report file, thesis file, incident doc). This sequence is preserved as the canonical pattern for the April 18–20 contamination episode.

### The two named failure modes

Apr 20 named two failure modes that the four events evidenced:

- **Social Proof Loop** — cross-engine amplification, where one engine's synthesis of another engine's interpretation compounds confidence without new evidence being introduced. The three-engine system (Founder / Systems / Divergent) was designed to prevent this. Events 1 and 2 are Social Proof Loop instances.
- **Intra-engine confabulation** — within-engine narrative generation that bypasses raw data, where a single engine reads ambiguous output and forward-propagates a confident interpretation without grep, file inspection, or independent recomputation. Events 3 and 4 are intra-engine confabulation instances. Apr 20's closing line: *"The three-engine system prevents cross-engine amplification (Social Proof Loop) but does not prevent intra-engine confabulation. Different defense needed."*

The defense pattern accumulation responding to intra-engine confabulation lives in Section 10's live-answer subsection ("intra-engine confabulation defense"). Patterns logged so far include the Reproducer Requirement (Apr 20), three-tier source tagging (Apr 20), Verification Output Safety (Apr 29), the Mixed-Source Synthesis Rule (Apr 29), and the Failure Escalation Protocol (Apr 29).

### Cross-cutting patterns extending the Apr 20 framing

Five patterns are named below as extensions of Apr 20's analysis. They are not new named failure modes; they are observations about how the two named modes manifest structurally and how remediation work behaves under contamination conditions.

**Pattern A — Compounding mechanisms hide in sort logic.** Apr 19 documented the commercialization-agent Social Proof Loop as one compounding channel: the THESIS_FILE write-back, where each run reads the prior day's thesis as input. The Apr 29 script audit (Section 4 correction; reproducer in `state_manifest.md`'s commercialization-agent entry: `sed -n '32,40p' experiments/week1/scripts/commercialization_agent.py`) revealed a second compounding channel that had operated invisibly the entire time: `load_latest(REVENUE_DIR)` at line 81 picks the lexicographically-greatest filename via `sorted(..., reverse=True)`. Because `commercialization_*` filenames always sort after `YYYY-MM-DD.md` in reverse-alphabetical order, this call returns commercialization-agent's own previous output whenever any commercialization output exists in the directory. The variable is named `prev_revenue` but the data is its own past output. Generalization for future audits: structural compounding mechanisms can hide inside utility functions whose names misleadingly describe a different role. Sort-logic, filename-prefix collisions, and mtime-vs-lexical ordering are the specific surfaces this case exposes; other agents may have analogous hiding surfaces not yet audited.

**Pattern B — Scope narrowness in remediation passes.** Remediation scope tracks the discovery surface, not the contamination surface. The Apr 18 sweep rewrote the agent in the active launchd pipeline (text_swarm) but missed `shadow_match.py` because `shadow_match.py` was not scheduled in launchd and did not surface during the production-pipeline review (Section 7 Finding 3). Apr 20 caught it. Apr 20's own grep was scoped narrowly to `policy_markets_seed`; the broader audit named in Apr 20 Question 1 ("Are there other agents referencing local seed files we haven't found?") never executed (Section 10 Apr 20 Item 4: "No evidence in any session log that the broader audit happened"). The April 18 sweep also missed `01_kalshi_selector.py`, found by Apr 20's grep — Apr 20 caught two seed-file loaders that Apr 18 had missed. Pattern: when a remediation pass is scoped to "the active pipeline" or "this specific search term," contamination outside that scope persists. *Update 2026-05-02:* The broader audit subsequently executed and returned a clean result — one orphan seed file found (`~/Projects/data/kalshi/policy_markets_seed.json`, hand-authored March 29, no active code loading from that path via direct reference), renamed to `.deprecated-orphan-2026-05-02` to fail-loud. All other `policy_markets_seed` references in the repo accounted for. See Section 10 Apr 20 Item 4 for the closure entry. The Pattern B observation as written remains the historical record of Apr 18 and Apr 20 sweep behavior; the specific open audit question Apr 20 Item 4 listed has been answered. The pattern itself — scope tracks discovery surface, not contamination surface — remains active.

**Pattern C — Filename is not behavior.** Section 7 Finding 4 names this directly: *"a script's name is not evidence of what it does; future audits should verify behavior, not infer from filename."* `01_kalshi_selector.py` was named "kalshi selector" but read `policy_markets_seed.json` and had nothing to do with Kalshi; the real Kalshi puller was a different script in a different directory. The same pattern shows up at the launchd boundary: the launchd job `com.latentforge.compression-researcher` invokes a script named `latent_compression_researcher.py` (filename mismatch documented in `state_manifest.md`'s compression-researcher entry). Two independent surfaces — script name vs. behavior, and launchd job name vs. invoked script — both demonstrate that names carry no enforcement of correspondence to code or behavior. Implication for future audits: identification of components must be by content inspection (`grep`, `cat`, behavioral test), not by name.

**Pattern D — Finding-real, prescription-wrong.** Section 6's introduction names this phrase directly: *"Apr 19's prescribed repair direction was, in three of four cases, later revised — finding-real, prescription-wrong."* Three Apr 19 prescriptions were superseded within 24 hours: (i) merging shadow_match's "battle-tested" search-term map into text_swarm was prescribed Apr 19 night; Apr 20's retraction of the "shadow_match pulling real prices" claim invalidated the assumption that the map was working ground truth (Section 10 Apr 19 Item 2 supersession). (ii) Prompt rewrites for revenue_strategist and commercialization_agent were prescribed Apr 19; the Structural-vs-prompt distinction (ChatGPT/Gemini, ratified Apr 19 itself) reframed the failure as structural protocol violation, not prompt bug (Section 10 Apr 19 Item 5 supersession). (iii) The benchmark report v0.2 was framed as a rewrite salvaging from `git checkout ac430e9`; `state_manifest.md`'s benchmark-updater entry reframed it as a first-time write of post-validation report-generation logic, because the April 19 cleanup had removed the generation logic entirely (Section 10 Apr 19 Item 7 supersession).

The Apr 19 manual pipeline simulation that produced these findings was a Founder Engine override — all four engines (Systems, Divergent, Analysis × 2) had converged on Path 3 (stop, verify at 4:40 AM April 20 automated run). The override produced seven concrete findings: three infrastructure repairs (broken Python syntax in `calibration_tracker.py`, poisoned `ANTHROPIC_API_KEY` in `~/.zshrc`, plaintext Kalshi key flagged) and four content failures (text_swarm matching, revenue_strategist contamination, commercialization-agent Social Proof Loop, calibration metrics). Catching these before the 4:40 AM automated run was the operational point. The pattern is therefore not that the override was wrong or any author's judgment was poor — the findings were real and the override was sound. The pattern is the structural difficulty of prescribing fixes under contamination conditions: prescriptions drafted while still inside the contaminated state can be built on the same Tier 3 inferences the contamination came from. The discipline implication: in a contamination response, finding identification and prescription writing should be temporally separated, with prescriptions held for next-session review against fresh ground truth.

**Pattern E — Runtime-input contamination as a propagation surface.** Apr 20's "artifact calcification" stage covered static artifacts: report files, thesis files, incident docs. The Apr 29 script audit (Section 5; specific dependency citations in `state_manifest.md`'s revenue-strategist and compression-researcher entries) revealed a second propagation surface that Apr 20's framing did not address: runtime inputs to the production pipeline. revenue-strategist's `load_brain_summary()` parses BRAIN.md sections (`## 1. THE THESIS`, `## 3. 90-DAY GOALS`, `## 4. WHAT WE ARE BUILDING`) into the agent's prompt context at every scheduled run. Where the read works (revenue-strategist), BRAIN.md's `[INVALIDATED 2026-04-18]` banner and surrounding pre-reset content enter agent prompt context daily. The compression-researcher case is a related but distinct finding: the script declares the same dependency but its relative-path bug means the read returns placeholder strings, so contamination does not propagate through that channel — the agent is context-blind instead, which is its own production-state problem (Section 5 audit finding) but not a Pattern E instance. Implication: the contamination surfaces an incident response must address include not only static artifact files but also files parsed at runtime by production agents. Apr 20's "artifact layer" framing should be read as a non-exhaustive enumeration of one propagation type, with runtime inputs as a separate type that requires separate audit.

**Pattern G — A well-formed response is not a correct one.** *(Minted September 19, 2026, on evidence dated April 20 through September 14. First Section 8 pattern that does not derive from the April 18-20 contamination; placed here because Section 8 is where named patterns live. Queued July 12 as "the inconsistent-view API pattern"; deferred three times.)* An external source can answer every question with a clean status code and a well-shaped body, and still be wrong, incomplete, or answering a different question than the one asked. Nothing in the response signals which. The evidence base is Polymarket's Gamma API, in two forms.

*Form 1: the response is silently wrong or incomplete.* `outcomePrices` arrives as a JSON-encoded string, not a list, and `current_price` does not exist; our type check failed quietly and every market fell through to 0.5 (April 20, Section 7 Finding 1). Querying by explicit condition_ids silently applies `closed=false`, so a resolved market returns `[]` rather than an error, and the loader's "closed" classification path was unreachable code as first written (July 11). Unknown parameters are ignored with HTTP 200 and unfiltered results; `limit` silently caps at 100; `liquidity_max` does not exist and is silently ignored; null-`endDate` markets are invisible to date filters in both directions; and the events endpoint serves inconsistent views, where events named as parents by live markets return zero rows on direct fetch (July 12, eight findings). `liquidity` and `volume` are strings while `liquidityNum` and `volumeNum` are floats, so fresh fetch logic read fifteen zeros that could have passed for a market collapse (September 14, findings file section 9). The one loud failure observed in this whole surface is HTTP 422 past the ~2000 offset ceiling.

*Form 2: the field is real but does not mean what the rule assumes.* `end_date` is not settlement date: four markets settling the same day were split across two months by a year-boundary stamp, one pick was stamped a day into the wrong month, and a 19-market family across nine products carried an identical placeholder date (September 14, F1, F4, F5). `liquidity` is not crowd participation: $114,465 of posted depth sat against $3,880 of lifetime trading, a trait of the whole question family (F2). The selection session applied rules 3a-3f correctly as written; the rules leaned on the field. Standing Founder decisions D1 (settlement date governs, derived from question text) and D2 (volume recorded beside liquidity) are the remedies already in force.

*What caught every instance was the same thing: an independent expectation to compare the response against.* The loader's Q5.1 identity check compares the returned conditionId set to the registry, and it is the sole guard against an empty-but-valid 200 (July 11 follow-up). The pool tool's completeness checks A and B compare the windowed traversal to an unfiltered reference walk, and both fired on real holes during development (July 12). The fifteen zeros were caught only by cross-checking against pool figures (September 14). The settlement errors were caught by reading the question text against the field. No instance was caught by the response itself.

*Implications for future work.* (1) A status code and a well-shaped body are not evidence. Any consumer of an external response needs an expectation of its own that can fail loudly: a count, an identity set, a reference walk, a second source. (2) Absence in a response is not absence in the world: `[]` for a resolved market and the invisibility of null-dated records are both the API declining to show something that exists. (3) A rule that names an API field is only as good as that field's semantics. Where a rule encodes intent (do not cluster expiries; require a real crowd), write the check against the intent and treat the field as evidence, not as definition (findings file section 8). (4) The repo's working code is the reference implementation: the User-Agent requirement and the Num-field variants were already solved in the loader when fresh fetch logic tripped on both. Borrow mechanics; do not rewrite them. (5) What cannot be reached cannot be proven absent: residuals beyond the offset frontier are documented as residuals, not assumed empty. *Not counted as an instance:* a community-made RSS feed that answered 200 with zero items, found September 19 in a non-Gamma source. It has the same shape, but it is same-day evidence and is recorded for a future reader, not used to mint. *Cross-references:* Section 7 Finding 1; July 11, July 12 and September 14 entries of this ledger; `founder_inputs/2026-09-14_gate4_findings.md` sections 3, 8 and 9. *Reproducers:* `curl -s "https://gamma-api.polymarket.com/markets?condition_ids=<resolved-cid>"` returns `[]`, and the same URL with `&closed=true` returns the market; `curl -s -o /dev/null -w "%{http_code}" "https://gamma-api.polymarket.com/markets?closed=false&limit=100&offset=2100"` returns 422; findings file sections 3 and 8 for Form 2.

**Pattern H — A failure handled as data reports success.** *(Minted September 19, 2026. Queued since May 9 as "wrapper-level silent success"; deferred May 9, July 12, and twice on September 19. **Minted inside the Pattern D window by Founder decision:** the third instance and the shared-root observation are both dated September 19. The Systems Engine flagged the conflict between the handoff's no-further-deferral rule and the 24-48 hour guard before drafting and offered three options; the Founder chose to mint. Insurance owed: a cold re-read of this pattern in a session dated September 21 or later, which may amend or withdraw it.)* A component hits a failure, converts it into an ordinary return value, and carries on. The run ends with exit 0, the wrapper stamps SUCCESS, the retry logic never engages, and what lands on the output path is either nothing or something shaped like output. Every layer did what it was built to do; the failure simply never reached a layer that could act on it.

*Three instances, one root.* **kalshi-pull** (occurred May 5-6, found May 9): a `try/except Exception` swallowed a DNS failure, the function returned normally, exit 0, `SUCCESS on attempt 1`, no data file, no retry. Fixed May 11 with `sys.exit(1)`. **research-sweep** (found July 12): bare `except: pass` in all three fetchers and silently dropped non-200s; empty results rendered as "No new results found."; five scheduled runs and one manual run wrote identical 1,711-byte "all quiet" digests. Fixed `755a79c`. Its partial-failure path then stayed exit 0: on September 13-15 every arXiv fetch failed, the digest printed the quiet-day sentence, and all three runs were stamped SUCCESS. Fixed `36a6fc5`. **compression-researcher** (found September 19): `call_claude` returned error text as its return value on all four failure paths and `main` wrote it to the output file; 105 files of error text over five months, every run stamped SUCCESS. Fixed `2832d02`. The three differ in layer (whole-script swallow, per-fetch count-and-discard, error string returned as research) and share the root: **the failure became a value, and the value was handled as data.** The third is the worst shape because the other two left the output path empty or bland, while this one put an error message where research was expected.

*Kin, not counted.* The April 20 `_extract_price` fall-through to a hardcoded 0.5 (Section 7 Finding 1) has the same root; "What Section 8 does not establish" below declined to name it separately at the time, and that paragraph is left as written. The July 11 zero-byte wrapper that ran as a silent no-op with exit 0 reports success on no work by a different route. *Counter-examples already in the repo:* the mode1 loader (tiered exits, ERROR sidecar, last good file preserved) and calibration-tracker (`return None` on full failure, `continue` on `None`, so a failed estimate is skipped and logged, never recorded as 0.5). Pattern G is the outward-facing twin: there the world answers wrongly while looking fine; here our own programs report fine while having failed.

*What has worked.* (1) A failure must change the exit code; the exit code is the only thing the wrapper can see. (2) Nothing is written to the output path on failure; the output path is where downstream trust lives. (3) Validate the output against the thing, not against the error strings seen so far: "does this look like research" (three `SUGGESTION`s) caught a 529 that a search for `Request failed` could not. (4) A SUCCESS stamp is not evidence of output. Check the output. Cheap detectors that found all three: identical file sizes across days (1,711; 219 and 411), and start-to-SUCCESS gaps that are physically implausible (one second, or many hours). (5) Every fix here was verified by forcing the failure on the host (WiFi off, May 11 and July 12; no key, September 19), not by reading the patch.

*Open, and not solved by any of the above.* **Loud into a void is not loud.** All three components now fail loudly into a `cron.log` nobody reads; research-sweep's July banner was correct and went unread for two months; six loader ERROR sidecars were seen on September 14 and waved through as a closed window without being opened. How a failure reaches the Founder is the unsolved half of this pattern and needs its own decision. *Also open:* kalshi-pull still treats an empty-data 200 as success (May 9 follow-on, deferred). No audit for this shape is recorded for the four unloaded components (text-swarm, benchmark-updater, revenue-strategist, commercialization-agent); checking each for except-swallow, count-and-discard, and error-as-return belongs on its reload prerequisites. *Cross-references:* May 9 audit findings; July 12 research-sweep entry; the three September 19 entries. *Reproducers:* `grep -c "except: pass" experiments/week1/scripts/research_sweep.py` returns 0; `grep -n -E 'return f?"(ERROR|API Error|Request failed)' experiments/week1/scripts/latent_compression_researcher.py` returns nothing; `grep -L "SUGGESTION" research/suggestions/*.md | wc -l` returns 104; `grep -n "sys.exit(1)" experiments/week1/scripts/kalshi_pull.py`.

### What Section 8 does not establish

The named-mode taxonomy for the April 18–20 contamination remains at two: Social Proof Loop and intra-engine confabulation, both named by the Apr 20 supplement. Patterns A through E are extensions, not new named modes. Two close-cousin concepts are canonically named elsewhere and not imported here: the Context-Filling Machine framing in `intent.md`'s Decision Rule for Design Changes and `state_manifest.md`'s session-tainting rule; and the "silent fallback" instance from Section 7 Finding 1, which is an instance of the four-stage sequence's "ambiguous output" stage rather than a separately-named pattern. Future readers asking which framing applies should consult the home document: intra-engine confabulation in Sections 8 and 10 of this ledger; Context-Filling Machine in `intent.md` and `state_manifest.md`. Defense pattern accumulation across both framings lives in Section 10's live-answer subsection.

---

## Protocols ratified during incident response

Chronological log of protocols ratified during the April 18–20 response. Some have since been consolidated, renamed, or absorbed into broader rules in `intent.md` or `state_manifest.md`. The current set of in-force protocols lives in `state_manifest.md` ("Operational protocols in force" section) and the four-protocol reasoning list in the Session-tainting rule.

### April 18, 2026 (in INCIDENT_2026-04-18.md, Section 6)

- **Evidence tiers.** Tier 0 (intuition) / Tier 1 (observed once) / Tier 2 (reproducible internally) / Tier 3 (externally defensible). Required tag on every claim entering BRAIN.md, outreach, or strategic documents.
  - *Lifecycle:* Superseded April 20 by the three-tier source tagging (`[VERIFIED_WITH_REPRO]` / `[OUTPUT_OBSERVED]` / `[INFERRED]`) and folded into the Ground Truth Hierarchy (Tier 1 / Tier 2 / Tier 3) in `intent.md`. The April 18 four-tier framing is no longer in force; the current three-tier hierarchy is.

- **Source-type tagging.** `[LOCAL_LOG]` / `[API_VERIFIED]` / `[DERIVED]`. Required on every metric.
  - *Lifecycle:* Superseded April 20 by the reproducer requirement (every claim carries a rerunnable command, not a tag). Source-type tagging is no longer in force.

- **Three-engine anti-Social-Proof-Loop protocol.** No engine synthesizes another engine's conclusions into BRAIN.md without Founder promotion. Disputed claims trigger git history check → raw data inspection → timestamp sanity → source freshness → independent recomputation, before new synthesis. Silent Operator mode for independent feedback.
  - *Lifecycle:* Partially in force. The "no engine synthesizes another engine's conclusions" rule is preserved in the Mixed-Source Synthesis Rule in `state_manifest.md`. The Silent Operator mode is informally in force but not codified.

### April 19, 2026 (in INCIDENT_SUPPLEMENT_2026-04-19_ARCHIVED.md)

- **Manual simulation as standard practice.** After any global annotation sweep or contamination cleanup, the full launchd agent sequence must be manually simulated via production wrappers before the reset is considered closed.
  - *Lifecycle:* In force as the Manual simulation rule in `state_manifest.md` ("Operational protocols in force" section).

- **Attested summaries rule (Gemini, pending tomorrow).** Data files passed between agents must carry validity headers (e.g. `[DATA_VALIDITY: INVALIDATED_PRE_RESET]`). Downstream agents must parse these headers and may not make high-confidence narrative claims when upstream data is tagged invalid.
  - *Lifecycle:* Marked "pending tomorrow" April 19; never operationalized as a header-and-parser system. The intent — preventing high-confidence narrative claims on invalid upstream data — is preserved in the Mixed-Source Synthesis Rule and the Failure handling rule in `state_manifest.md`. The specific header format is not in force.

- **Structural vs prompt distinction (ChatGPT, ratified by Gemini).** Narrative agents synthesizing misleading claims from real data is a structural protocol violation, not a prompt bug. Fix is evidence-tier enforcement and claim validation, not prompt tweaks.
  - *Lifecycle:* In force as a framing principle, not a codified rule. Reflected in the structural-fix requirements named in `state_manifest.md`'s commercialization-agent entry (split output directory, filter by prefix, mtime-based selection — none of which are prompt fixes).

### April 20, 2026 (in INCIDENT_SUPPLEMENT_2026-04-20_ARCHIVED.md)

- **Reproducer requirement.** Every claim in an incident doc, state manifest, or strategic artifact must include a command or file-path check that a fresh agent could rerun. Claims without a reproducer are `[INFERRED]` by default, not `[VERIFIED]`.
  - *Lifecycle:* In force as the Reproducer requirement in `state_manifest.md` ("Operational protocols in force" section). Extended April 20+ to cover transformation logic, not just claim verification (per the April 20 text-swarm `_extract_price` failure).

- **Three-tier source tagging.** `[VERIFIED_WITH_REPRO]` / `[OUTPUT_OBSERVED]` / `[INFERRED]`. Treats `[OUTPUT_OBSERVED]` as distinct from `[VERIFIED]`.
  - *Lifecycle:* In force as a tagging convention in incident docs. Folded into the Ground Truth Hierarchy in `intent.md` (Tier 1 raw API data ≈ `[VERIFIED_WITH_REPRO]`, Tier 2 empirical system logs ≈ `[OUTPUT_OBSERVED]` for reproducible measurements, Tier 3 agent syntheses ≈ `[INFERRED]`).

- **Hardcoded narrative lines in output files prohibited.** Scripts may not write claims about system state into their own output files without those claims being dynamically computed and verifiable.
  - *Lifecycle:* In force as a code-review principle. Not codified as a separate rule but enforced through script audits (per April 29 audit findings in this ledger).

- **"Clean-looking wrong output" principle.** When a system is known-broken, quarantine the scheduled job rather than let it produce polished failure.
  - *Lifecycle:* In force as standard practice. Applied April 20 (text-swarm unloaded), April 19 (commercialization-agent unloaded), and ongoing — current launchd state per `state_manifest.md` reflects this principle (failed components unloaded, not allowed to produce contaminated output).

---

## Forward action items

The list below reconciles the Apr 19 and Apr 20 supplement pending-work lists against current state, marks each item resolved/open/absorbed, and preserves the existing Apr 29 Kalshi credential audit. Entries use *Reproducer:* for rerunnable commands and *Tracking:* for pointers to where ongoing work is recorded in `state_manifest.md` or `intent.md`. *Meta-observation: the Apr 19 supplement was caught between Apr 18's contamination discovery and Apr 20's deeper findings — most of its forward plans got revised within 24 hours, so multiple Apr 19 items below are marked superseded by Apr 20 reframings rather than carried forward as written.*

### Apr 29 2026 — Audit stored credentials

- **Apr 29 2026 — Audit stored credentials.** Audit all stored credentials (env files, dotfiles, Keychain) against active script dependencies. Remove orphaned secrets. Recurrence: at incident-ledger entry boundary or quarterly.
  - *Trigger: Apr 29 2026 Kalshi key near-miss revealed a plaintext key (`KALSHI_API_KEY` in `~/.zshrc` line 4) that had been doing nothing for weeks. Script accesses public endpoints only; no authentication required.*
  - *Mitigation completed Apr 29 2026:* Old key revoked on Kalshi, plaintext line removed from `~/.zshrc`, backup saved as `~/.zshrc.backup-2026-04-29`, `kalshi_pull.py` confirmed working without authentication (1,000 markets pulled successfully).

### Apr 19 2026 — Pending work reconciled

Original list from `INCIDENT_SUPPLEMENT_2026-04-19_ARCHIVED.md`, "Pending work (handoff to April 20 session)" section. Priority order preserved.

1. **Four-point check on 4:40 AM automated run** — `[ABSORBED]`. One-time verification gate for the morning of April 20. Window passed. The bugs this check was meant to catch (text_swarm extraction failure) were discovered on April 20 through other means; the protocol concern (live-data ingestion correctness) is now an ongoing operational matter, not a discrete pending action.

2. **Merge `shadow_match` search-term map into `text_swarm.load_live_markets()`** — `[OPEN, but superseded]`. Original plan assumed shadow_match's search-term map was working ground truth. April 20 supplement retracted that view (shadow_match was loading the seed file all along; its "real prices" claim was wrong). The underlying open work — text_swarm needs working matching logic — is now tracked as Apr 20 Pending Item 1 (define matching contract first), not as the merge-from-shadow_match plan written here. *Tracking:* `state_manifest.md` text-swarm component entry, `Issue: matching logic broken (bimodal fallback output); pending matching contract`.

3. **Re-evaluate calibration validity once text_swarm is producing real crowd baselines** — `[OPEN]`. Dependency on Item 2 still holds. *Tracking:* `state_manifest.md` calibration-tracker component entry, `VALID: no, Issue: scoring against contaminated or fallback baselines; pending text-swarm matching fix`.

4. **Design attested-summaries protocol (header format, validator logic, agent-prompt enforcement)** — `[ABSORBED, soft]`. Specific protocol design (validity headers + parser logic + prompt enforcement) was never built. The intent — preventing high-confidence narrative claims when upstream data is invalid — is preserved differently in the Mixed-Source Synthesis Rule and Failure handling rule in `state_manifest.md`. If a future decision determines the header-and-parser approach is needed, this re-opens as a discrete item.

5. **Rewrite revenue_strategist + commercialization_agent prompt templates under new protocol** — `[OPEN, framing shifted]`. Original Apr 19 framing called for prompt rewrites. That framing was explicitly rejected on Apr 19 itself by the Structural-vs-prompt distinction (ChatGPT/Gemini): "narrative agents synthesizing misleading claims from real data is a structural protocol violation, not a prompt bug." The actual remaining work, per `state_manifest.md` commercialization-agent entry, is structural — split commercialization output into a separate directory, modify `load_latest` to filter by filename prefix, replace reverse-alphabetical sort with mtime-based selection. revenue-strategist's open work is also structural (BRAIN.md runtime-input dependency, template contamination). A future reader looking at this entry should treat "rewrite the prompts" as the *wrong* path — structural fix is the actual path. *Tracking:* `state_manifest.md` revenue-strategist + commercialization-agent component entries.

6. **BRAIN.md narrative rewrite grounded in April 20 live data** — `[OPEN, with runtime-input nuance]`. Apr 19 framed this as a narrative/prose update. The Apr 29 incident-ledger audit findings reframed it: BRAIN.md is a runtime input to two production agents (revenue-strategist, compression-researcher) that parse specific sections (`## 1. THE THESIS`, `## 3. 90-DAY GOALS`, `## 4. WHAT WE ARE BUILDING`) into agent prompt context. So a "rewrite" is not just human-reference documentation; it edits agent behavior. *Tracking:* `state_manifest.md` (revenue-strategist + compression-researcher component entries documenting BRAIN.md runtime dependency) and `incident_ledger.md` (April 29 audit finding on BRAIN.md as runtime input).

7. **v0.2 clean rewrite salvaging technical content from PRE_RESET_DRAFT** — `[OPEN, framing shifted]`. Original framing: "rewrite" salvaging from a prior draft. State_manifest.md benchmark-updater entry reframes: the April 19 cleanup removed the report-generation logic; the script is now scaffolding-only with validation gates in front of empty implementation. The actual work is a *first-time write* of post-validation report-generation logic, not a rewrite. A future reader should not `git checkout ac430e9` looking for code to salvage as the Apr 18 doc's Section 5 plan suggested — that approach has been superseded. *Tracking:* `state_manifest.md` benchmark-updater component entry.

8. **Bearish asymmetry re-validation (Gemini's flag: may be proxy contamination)** — `[OPEN]`. *Tracking:* `intent.md`, "What is under re-audit" section: "The bearish asymmetry finding from April 17 may be proxy contamination from the synthetic seed file invalidated on April 18, not real directional physics. Flagged by Divergent Engine (Grok). Requires re-test against live data."

9. **Move Kalshi key to Keychain** — `[RESOLVED, in different shape]`. Solved by deletion rather than migration on Apr 29 2026 — the script accesses public endpoints only, no authentication required. *Reproducer:* `grep -c 'KALSHI_API_KEY' ~/.zshrc` returns 0; `~/.zshrc.backup-2026-04-29` preserves the pre-Apr-29 file with the line; `kalshi_pull.py` runs without authentication.

10. **Mac Mini bidirectional steering work — only after 1-8 stable** — `[ABSORBED → intent.md Mac Mini Autonomy Rule]`. Per `intent.md`: "Mac Mini work is Founder-initiated only. Fresh Claude sessions must NOT propose using the Mac Mini autonomously — even if the MacBook Air hits a capacity bottleneck. Claude requests Mac Mini work; Claude does not propose it." This item is no longer Claude's to track in a remediation ledger. Current state of the work itself is not evaluated here.

### Apr 20 2026 — Pending work reconciled

Original list from `INCIDENT_SUPPLEMENT_2026-04-20_ARCHIVED.md`, "Tomorrow's first tasks" section plus Question 3 from "Questions for the team." Priority order preserved.

1. **Define matching contract before writing any matching code** — `[OPEN]`. This is the supersession target for Apr 19 Item 2. Substantive nuance: the work is contract design first (what counts as a match? required keyword set? scoring with threshold? test cases that prove or disprove correctness?) and code second. The previous matching code mapped every benchmark question to "Ethereum intraday" or "Fiorentina" because it had no formal notion of correctness. *Tracking:* `state_manifest.md` text-swarm component entry: `Issue: matching logic broken (bimodal fallback output); pending matching contract`.

2. **Implement matching per contract; verify against current live JSON** — `[OPEN, dependent on Item 1]`. Original Apr 20 phrasing referenced "verify against live 2026-04-21.json" — that date anchor is stale; verification will happen against whatever live JSON exists when implementation lands. *Tracking:* same `state_manifest.md` text-swarm entry.

3. **Rewrite shadow_match.py — point at live JSON, reuse `_extract_price()`** — `[OPEN]`. *Tracking:* `state_manifest.md` shadow_match component entry: `[not in launchd | VALID: no]`, `Issue: still reads quarantined policy_markets_seed.json; pending rewrite to live-data source`.

4. **Grep repo for seed references; delete quarantined seed file if clean** — `[Part 1 RESOLVED 2026-05-02, Part 2 OPEN]`. Apr 20's narrowly-scoped grep caught only `policy_markets_seed` references. Apr 20 Question 1 explicitly flagged that a broader audit was needed: "Are there other agents referencing local seed files we haven't found?" No evidence in any session log that the broader audit happened. Part 2 (delete the quarantined seed) depends on Part 1. *Reproducer:* `ls docs/INCIDENT_ARCHIVE/policy_markets_seed_DEPRECATED_DO_NOT_LOAD.json` shows the seed JSON still present.

   *Closure (Part 1) 2026-05-02:* Broader audit executed via `grep -rn "policy_markets_seed" ~/Projects/latentforge-latentmas/`. Result: one orphan seed file found at `~/Projects/data/kalshi/policy_markets_seed.json` (889 bytes, dated March 29, 2026 12:08, hand-authored fictional data with the same shape as the original contamination root cause). Verified no active code in the repo loads from that path via direct reference. File renamed to `policy_markets_seed.json.deprecated-orphan-2026-05-02` to fail-loud on any future load attempt. All other `policy_markets_seed` references in the repo accounted for: (a) `experiments/benchmark/shadow_match.py` line 23 (already known per Section 7 Finding 3, not in launchd, pending rewrite); (b) documentation and audit references in Trinity files and archived incident docs. *Reproducer:* `grep -rn "policy_markets_seed" ~/Projects/latentforge-latentmas/ --include="*.py"` should now return only `shadow_match.py`'s line 23 reference; `ls ~/Projects/data/kalshi/policy_markets_seed*` should show the `.deprecated-orphan-2026-05-02` file and not the original. Part 2 (delete the quarantined seed at `docs/INCIDENT_ARCHIVE/policy_markets_seed_DEPRECATED_DO_NOT_LOAD.json`) is now unblocked but not yet executed; deferred for a separate decision.

5. **Decide fate of `01_kalshi_selector.py`** — `[RESOLVED]`. Deleted. *Reproducer:* `git log -- experiments/benchmark/01_kalshi_selector.py` returns commit `452633d chore(benchmark): delete 01_kalshi_selector.py`; `ls experiments/benchmark/01_kalshi_selector.py` returns `No such file or directory`.

6. **Reload text-swarm into launchd only after Items 1-3 verified** — `[OPEN, blocked on Items 1, 2, 3 above]`. *Tracking:* `state_manifest.md` text-swarm component entry: `[LOADED: no | VALID: no]`.

### Live answer to Apr 20 supplement's Question 3 — intra-engine confabulation defense

Apr 20 supplement closed with three open questions for the team. Question 3 — *"The three-engine system needs an intra-engine-confabulation defense. The reproducer requirement is step one. What else?"* — has a live answer in progress: defense patterns being accumulated across April 20+ sessions. Patterns logged so far include the Reproducer Requirement (Apr 20), three-tier source tagging (Apr 20), Verification Output Safety (Apr 29), the Mixed-Source Synthesis Rule (Apr 29), and the Failure Escalation Protocol (Apr 29). These collectively address the failure mode Apr 20 named (engine reads ambiguous output → generates confident narrative → forward-propagates without returning to raw data). Consolidation into `intent.md` will happen when the pattern set stabilizes; until then, the live record lives across `intent.md`, `state_manifest.md` ("Operational protocols in force" section), and the Apr 29 audit findings in this ledger.


- **July 12, 2026 — Gamma API traversal pathologies characterized (eight findings, Tier 1); v3 event-windowed pull tool built; fresh qualifying pool generated (Gate 2 of v2 selection closed).** During Gate 2 of registry v2 selection (per `founder_inputs/2026-07-12_v2_selection_session_handoff.md` and its Gate 2 continuation), the May 26 pool tool's traversal was found non-viable and the Gamma API's traversal surface was characterized by direct probe. Findings 1-4 are from selection session #1 (same day, morning/afternoon); findings 5-8 from the continuation session (evening). All probe-verified; reproducers below. Shared trait with prior Gamma findings (July 11 closed-filter entry): the API fails silently — HTTP 200 with unfiltered results on unknown parameters, silent caps, inconsistent views — with one exception (finding 1, the API's first observed loud failure).
  1. *Offset ceiling ~2000.* `offset=2100` returns HTTP 422 on both `/markets` and `/events`. Live-market universe >4000; live-event universe >9500 (window-union count 9,562 this session; frontiers do not meet on either endpoint). *Reproducer:* `curl -s -o /dev/null -w "%{http_code}" "https://gamma-api.polymarket.com/markets?closed=false&limit=100&offset=2100"` -> 422.
  2. *No cursor pagination.* `id_min`, `min_id`, `start_id`, `id_gt`, `id.gt`, `after`, `from_id` all silently ignored (HTTP 200, unfiltered results). `id=N` is exact-match only.
  3. *`limit` silently caps at 100.* Requested 500, got 100, no error.
  4. *Working filters (probe-verified by content):* `end_date_min`/`end_date_max` (including sub-day ISO timestamps — verified by disjoint half-day result sets on 2026-07-13), `liquidity_min`, `order=id|endDate` with `ascending`. `liquidity_max` does NOT exist (silently ignored; returned liquidity up to $7.8M under `liquidity_max=500`).
  5. *Null-endDate markets invisible to date filters in both directions.* `end_date_min=1970-01-01` excludes them (session #1 finding); `end_date_max=1970-01-01` returns zero rows (continuation probe). Null fails every date comparison. Known specimens: market ids 694160, 814612, 943819 (fetchable by exact-match id, all carrying real liquidity values).
  6. *Null-endDate EVENTS exist:* 15 in the reachable first 2100 (ids 57681-~300526, all old/low-ID range). Discovered by completeness check A failing loud on first full v3 run — the check design anticipated exactly this. Date-windowed event traversal cannot see them; remediated by merging them from the unfiltered reference walk.
  7. *Events endpoint serves inconsistent views (headline finding).* Events 66119, 84383, 84390 are claimed as parents by live markets (full event objects embedded in market-side responses) but return ZERO rows on direct `/events?id=N` fetch. Event 34436 is `closed=true` while containing a live market (568695). Consequence: event traversal alone is structurally incomplete — some live markets' parents are unfetchable from the events side. 9 such markets in the reachable first 2100 (~0.4%). *Reproducer:* `curl -s "https://gamma-api.polymarket.com/events?id=84383"` returns `[]`; `curl -s "https://gamma-api.polymarket.com/markets?id=689015"` shows event 84383 embedded with full details.
  8. *Transient IncompleteRead failures on large chunked responses* (~1 per few hundred requests). Remediated with bounded page-level retry (3 attempts), preserving fail-loud after exhaustion.

  **Tool:** `scratch/full_pull_and_filter_v3.py` (hash of record `baedffec...`, commit `e1b19af`). Traversal: 15 half-open date windows over `/events` (each pre-probed at offset 1900 for overflow; in-script guard re-probes at runtime and fails loud naming any overflowing window), plus null-event merge from unfiltered reference walk, plus market-level backfill for invisible-parent cases. Two completeness checks, both fail-loud with no-output-written semantics: A (every reference-walk event in window union), B (every reference-walk market in harvest). Both checks fired correctly on real holes during development before passing. Filter internals imported unchanged from v2 (hash `a96d13a9...`). Window map note: the 2026-07-13 single day required sub-day splitting (>1900 live events resolving that day — the intraday contract stack).

  **Output:** `scratch/qualifying_pool_2026-07-12.json` (hash `e5228143...`, 4,040 markets, 77 open-ended, commit `e1b19af`). Pipeline: 9,562 events -> 78,134 unique markets -> 4,040 qualifying. Pool is a timestamped snapshot of a moving surface (window counts shifted between same-evening runs as markets opened/resolved).

  **Known residual (documented, not hidden):** null-endDate events or invisible-parent markets with IDs beyond the 2000-offset frontier are unreachable and their absence unprovable. All observed instances of both classes sit in old, low-ID ranges (nulls: 57681-~300526; invisible-parents: similar), consistent with legacy artifacts. Check B is the standing instrument that detects either class growing; backfill count (currently 9) is the audit trail.

  **Session notes:** two paste-echo false alarms (terminal rendering, verified whole by content checks); one real shell failure (zsh word-splitting in a multi-variable loop — multi-variable shell loops now on the same content-verification footing as heredoc pastes); one session #1 finding (5's magnitude estimate "dozens-to-hundreds") corrected by measurement to 15 events / 77 qualifying open-ended markets. Pattern-minting on the inconsistent-view finding deferred to a fresh session per Pattern D discipline.

- **September 14, 2026 — Gate 4 cold re-read closed; registry v2 locked and loader repointed; registry v1 archived.** The CANDIDATE committed July through September was **NOT CONFIRMED as submitted**; confirmed after two Founder-approved replacements. Six findings, full record in `founder_inputs/2026-09-14_gate4_findings.md` (sha256 `c5745c36...`, commit `55314e2`).

  **Headline finding: every rule in 3a-3f is enforced against a Gamma API field, and two of those fields do not mean what the rules assume.** `end_date` misrepresented settlement on three separate surfaces — a four-market cluster all settling Dec 31 2026 but split 2/2 across "December" and "January" by the year boundary (two are phrased as `before 2027` / `in 2026` and carry a 2027-01-01 stamp); a Bitcoin market stamped 2026-10-01 that settles September 30; and an entire 19-market crypto-launch family across nine products carrying an identical placeholder `end_date` of 2028-01-01, making their settlement month unknowable. `liquidity` measured quoted depth rather than crowd participation — one pick carried $114,465 posted against $3,880 lifetime volume, and its two sibling ceasefire markets scored volume/liquidity of 1.1x and 0.8x, confirming a family trait rather than a one-off. Joins the standing Gamma cautions: `endDate` unreliable (June 27, 2026), silent closed-filter (July 11), eight traversal pathologies (July 12). *Reproducer:* findings file sections 3 and 8.

  **Founder decisions.** **D1 (standing, all future selections):** 3b is read semantically — settlement date governs, derived from question text wherever the field disagrees; markets with unknowable settlement dates are not selectable. **D2 (standing):** volume is recorded alongside liquidity for every pick; a liquidity number without trading volume is not a crowd. **D3:** 3f block corrected — geopolitics is 8/15, not 7, and the declared counts had summed to 14; all four correlations named explicitly. **D4:** Strait of Hormuz dropped as the Dec-31 settler, resolving the cluster and the undeclared Iran correlation trio in one move; cost accepted on record ($11.7M lifetime volume, and the registry's only other-commodities pick). **D5:** replaced with skin cancer vaccine FDA approval and Milei 2027 Argentina, both cited from the verified pool, never from engine general knowledge; Milei chosen over an Alito SCOTUS alternative, returning geopolitics to 53% as an accepted cost.

  **Outcome.** Live verification against the Gamma API: 15/15 open and accepting orders, all above the $10K floor (lowest $21,427, Milei), all volume/liquidity above 1.0 (lowest 2.1x, highest 100.3x), liquidity drift from selection -13% to +14%. Registry v2 locked (`c4717c68...`, commit `1665078`); loader repointed (`9cc65738...`, same commit); live run returned `ALL_LIVE live=15 retired=0` exit 0 — the first exit 0 since the June 30 v1 retirements, with v1 having fallen to 1 live, 7 retired. Registry v1 archived (`e6d7b3b`) with the ratified July template plus handover state; no in-file banner added, per the April 18 finding that contamination resistance comes from segregation rather than warnings, and because v1 is superseded rather than contaminated. Accepted tradeoffs on record: geopolitics 8/15 (53%); three markets settling simultaneously on 2026-12-31, at the 3b cap; near-term attrition with Bitcoin at 16 days and Bolsonaro at 19, reaching 13 live by early October and making a v3 conversation likely around January 2027.

  **Gate-design gap (new, not previously recorded).** No gate checked the candidate against the loader's **input contract**. Gates 1-4 cover selection rules (3a-3f) and market liveness only. The promoted v2 was built in the pool-record shape — `registry_version` instead of `version`, `conditionId` instead of `condition_id`, with `registry_index` and `selection_snapshot` absent entirely — and would have hard-failed the loader on version mismatch, or thrown KeyError after. Caught before the repoint only by reading the loader's source rather than assuming its interface. **A schema-conformance check belongs in the gate sequence before any future promotion.** Related: two live-check failures the same session came from writing fresh fetch logic instead of borrowing the loader's — Gamma returns 403 without a real User-Agent (documented at loader line 64), and `liquidity`/`volume` are strings while `liquidityNum`/`volumeNum` are floats. Both traps were already solved in the file being repointed. *Reproducer:* findings file section 9.

  **Carried forward, still unwritten.** From the September 14 selection session: open-ended/null-endDate bucket contamination (Nov-2026 midterm markets carrying null `endDate` with roughly seven-week runway, so 3c was satisfied from dated 180d+ markets only); recurring-market churn doubled July to September with the event universe at ~21K, absorbed by the auto-split traversal in `full_pull_and_filter_v3_1.py`. Pattern-minting queue unchanged and now twice deferred: the inconsistent-view API pattern, and the research-sweep silent-success second instance. Finding F5 and the root-pattern section of the September 14 findings file are relevant input to the first of those.

- **September 14, 2026 — registry selection gates promoted from one-off to standing procedure; schema-conformance gate added.** The v2 selection gates (July 12 handoff) were written for that version only and existed in canon solely as a parenthetical in the July 12 entry above — the selection *rules* (3a-3f) were locked as standing, but the procedure that verifies them was not. Founder decision: promote, on the evidence of the one completed run. Generalized into four gates in `state_manifest.md` ("Operational protocols in force"): fresh pool; known loader defects ship before lock; **schema conformance** (new); candidate-then-confirm. Gate 3's specific `limit`-param fix is closed and was generalized rather than dropped, since the shape recurs. Gates are named by function, not number — the historical "Gate 3 hardening" and "Gate 4 cold re-read" labels in canon refer to the v2 instance and are unchanged. The new schema gate closes the gap recorded in the September 14 Gate 4 entry above: no gate checked the candidate against the loader's input contract, and the promoted v2 was built in pool-record shape that would have hard-failed on version mismatch. Correction to the July 12 entry's framing: it describes "four gates (rules-first, fresh Tier 1 pool, loader limit fix pre-lock, candidate-then-confirm)" — the source handoff has three gates, numbered 2 through 4, with rules-first being the locked-decisions section they operate under, not a gate. *Reproducer:* `grep -n "Registry version selection procedure" docs/state_manifest.md`; source at `founder_inputs/2026-07-12_v2_selection_session_handoff.md`.

- **September 14, 2026 (late evening) — loader input contract written; stale v1 text corrected; Systems Engine inventory-from-a-window failure.** Two commits. `29893e5` adds `docs/registry_input_contract.md` (hash `4a5da92a...` after the same-session amendment below), which records what `04_market_state_loader.py` actually enforces on a registry: five hard checks (version exact-match, markets-is-list, count 15, condition_id non-empty, condition_id unique) and four fields passed through unvalidated (`slug`, `registry_index`, `question`, `selection_snapshot`). Written so the schema-conformance gate ratified earlier today is checkable without re-deriving the contract from source each selection. **The doc deliberately inventories both halves of the registry** — the 7 per-market and 14 top-level governance fields the loader never reads are listed as inventory-only, meanings unverified, because a contract stating only the loader's requirements would license a future version that loads cleanly with its entire provenance record shed. Governance field meanings are NOT established by this session. `f18a51c` corrects nine strings in the loader that said "8 markets" or "v1 registry" while enforcing 15/v2 since this morning's repoint; two were error messages that would have named the wrong requirement to an operator mid-failure. Text only, no logic. Verified ast.parse clean, `--selfcheck` v2/15/15, live fetch 15 markets merged. Loader hash `9cc65738...` -> `aa8e59cd...`; manifest hash-of-record and registry-state reproducer updated in the same commit.

  **Method failure (the finding worth keeping).** The Systems Engine enumerated the stale strings from a `sed -n '95,195p'` window and presented the resulting seven-item list as the complete set. It was a window, not an inventory. A repo-wide grep after the first patch found three more at lines 83, 93 and 403, outside the window and never seen. The engine had *proposed* that grep a turn earlier and then drafted the list without running it. This is Pattern C applied to audit method rather than to filenames: identification by the view at hand rather than by search over the whole surface. It is the third small instance in one day — the evening session logged two figure-misattributions caught by grep — and all three share the shape of a claim asserted without returning to source. No harm reached canon: the doc's caution paragraph was amended in the same session so it records the correction rather than asserting strings are still stale. *Cheap standing defence, offered not ratified:* when enumerating occurrences of anything, the enumeration is a grep over the file, never a reading of a window.

  **Deliberately left.** Loader line 179 still describes the pre-July-11 single-call fetch ("ONE bulk call ... for all 8 markets"), superseded by the two-pass design; it needs a rewrite of the architecture description, not a number swap, and is scoped as separate work. Ledger line 388 keeps `9cc65738` because it records the state at the Gate 4 close — history, not current state. *(Update September 19, 2026: Pattern G was inserted into Section 8 above, which moved that line; it was line 398 when this note was written, and it moves again whenever the file grows above it. Find the line by content instead: `grep -n '^  \*\*Outcome\.\*\* Live verification' docs/incident_ledger.md`.)*

  **Repo-wide sweep, same session — clean.** Prompted by the method failure above, the search was widened past the one file already known to be affected (the narrow grep repeated the Pattern B shape: scope tracking the discovery surface). Roughly 60 hits across `.py`, `.md` and `.sh`, all correctly accounted for: the large majority are historical records of v1's selection and operation (`founder_inputs/` May-July, the v1 archive note, July 11 ledger entries, pre-reset `revenue_ideas/` files) which are true as written and must not be edited; the rest are tonight's own entries and two present-tense manifest lines that correctly name v2/15. **Exactly one live item repo-wide: loader line 179**, already scoped as separate work. No other file claims the system works a way it does not. *Limit of this result:* the sweep matched v1-flavoured strings only — a document describing current behaviour wrongly in different words would not appear. It rules out one class of staleness, not staleness generally.

  *Reproducers:* `shasum -a 256 experiments/benchmark/04_market_state_loader.py` returns `aa8e59cd...`; `grep -n "8 markets\|8 condition_ids\|v1 registry" experiments/benchmark/04_market_state_loader.py` returns line 179 only; `git show --stat f18a51c`; repo sweep `grep -rn "8 markets\|8 condition_ids\|registry_v1\|benchmark_registry_v1\|v1 registry" --include="*.py" --include="*.md" --include="*.sh" . | grep -v "INCIDENT_ARCHIVE"`.

- **September 15, 2026 — six stale single-call strings in the loader; last night's enumeration defence refined.** Loader line 179 (the module-level Piece 3 block) still described the pre-July-11 single-call fetch and was rewritten to the two-pass design, with the silent closed-filter reason recorded inline so a future reader is not tempted to collapse it back. Five further stale strings surfaced during verification, each caught by a grep run *after* the previous patch: line 72 (retry policy), lines 6 and 18 (module docstring summary and piece checklist), line 5 (`8-market`, hyphenated), line 56 (input description). Six strings, six distinct phrasings of the same fact. Text only, no logic. Verified ast.parse clean, `--selfcheck` v2/15/15, live two-pass fetch merged 15 markets. Commit `247ca8e`; hash `aa8e59cd...` -> `32197ee0...`.

  **The refinement.** The September 14 sweep was a grep over the whole repo, not a window — it satisfied last night's proposed defence and still missed five of six. Its defect was not scope but **pattern**: it enumerated one *phrasing* of the concept (`"8 markets"`, with a space) rather than the concept, which is why the hyphenated `8-market` on line 5 survived. Last night's caveat predicted exactly this ("a document describing current behaviour wrongly in different words would not appear") and the prediction held within 24 hours. The defence as offered — enumerate with a grep, never a window — is necessary but insufficient. The stronger form: **the grep pattern must be written against the thing being described, not against the wording last seen.** Still offered, not ratified.

  **Recorded for future sweeps:** `v1` in this file means two unrelated things — the registry version, and `LOADER_SCHEMA_VERSION = "mode1-loader-v1"` (lines 3, 43, 324, 511), the loader's own output schema. A sweep that treats them as one corrupts the schema version. Line 225's `fetch_markets_raw` docstring says "ONE bulk call" and is correct at function scope; deliberately unchanged.

  **Cleared, not findings:** `swarm_cron.log` carries 50 uncommitted lines ending Apr 20, 2026 — historical churn, `launchctl list | grep -i swarm` returns nothing, text-swarm genuinely unloaded, manifest correct. **Still open:** uncommitted churn in `calibration/cron.log`, `research/daily-digest/cron.log` and `research/suggestions/cron.log`; the latter two bear on the uninvestigated Aug 11 - Sept 8 research silence. *Reproducers:* `shasum -a 256 experiments/benchmark/04_market_state_loader.py` returns `32197ee0...`; `grep -n -iE "\b(8|eight)[- ](market|condition|cid)" experiments/benchmark/04_market_state_loader.py` returns nothing.

- **September 19, 2026 — compression-researcher has written error text to its output path in 104 files, reporting SUCCESS on every run; research-sweep arXiv fetch degrading. Third silent-success instance.** Surfaced while scoping the backlog item "uninvestigated four-week research-sweep/compression-researcher silence, Aug 11 to Sept 8." The gap itself resolved to a shared-host cause and is not the finding.

  **Finding 1 — compression-researcher writes exceptions as research output.** `research/suggestions/` contains 104 files whose body is an error string, not a suggestion. Today's file in full, after the header: `Request failed: HTTPSConnectionPool(host='api.anthropic.com', port=443): Read timed out. (read timeout=300)`. Consecutive files are byte-identical apart from date and generation timestamp (`diff` of 2026-09-18 vs 2026-09-19 returns two lines). Two error shapes by size: 82 files at 219 bytes (the 300s read timeout) and 22 at 411 bytes (a second, longer error, content not yet read). Real output exists only in a handful of 6,000-7,000-byte files. The script catches the exception, writes it to `OUTPUT_FILE`, and exits 0; the wrapper stamps SUCCESS and the 3-attempt retry never engages. Scale is at least three months — the earliest affected date has not been established, only that 104 files are affected.

  **This verifies, and understates, the April 29 script-level claim.** The manifest's compression-researcher entry records that the agent runs context-blind under launchd (DIGEST_DIR and BRAIN_PATH resolve to placeholder strings), explicitly tagged *"script-level inference, not run-level verification"* with run-level verification *"queued as a future audit item."* That item is now answered from its own output: the agent is not producing degraded output from placeholder context, it is producing no output at all. Whether the timeout is caused by the context-blindness, by prompt size, by model identifier, or by something else is **not established** and is not inferred here.

  **Finding 2 — research-sweep partial-failure path is silent in practice.** The July 12 fix works as built: total failure exits 1 and the wrapper retries (observed Sep 13, 15, 16, each recovering on attempt 2), and partial failure writes a bold in-digest WARNING banner (observed at the head of the 2026-09-14 digest: 14 of 19 fetches failed). But partial failure still exits 0 and stamps SUCCESS. `fetch failures` counts have risen from near-zero to a routine 3-5 of 19, and **arXiv hits were 0 on September 13, 14 and 15** — three consecutive days with zero papers from the feed whose purpose is catching thesis-threatening publications — all three stamped SUCCESS. The warning was written correctly and went into a file nobody opened. Consecutive digests of identical size (3,483 bytes on Sep 14 and Sep 15) recur as the same fingerprint that identified the July template outputs. Cause of the arXiv failures not investigated.

  **Third silent-success instance; the deferred minting decision now has its third case.** kalshi-pull (May 9, whole-script exception swallow), research-sweep (July 12, per-fetch swallow), compression-researcher (September 19, exception written to output path). The May 9 entry deferred pattern-minting pending a second instance; the July 12 entry deferred pending a fresh session, noting two instances to weigh. There are now three, and the third is the most consequential: the other two produced *empty* output that read as "nothing found," while this one produces output that is an error message occupying the place where research is expected. Minting decision remains deferred per Pattern D — this is the discovery session. It joins a pattern-minting queue now four deep (inconsistent-view API; research-sweep silent-success; the enumeration defence of Sept 14-15; this).

  **Deliberately not done tonight.** No fix designed, no cause diagnosed, no script read. Both components remain as they are. Per Pattern D, prescriptions drafted inside the session that surfaced the finding are the named failure shape; this entry records evidence only. `state_manifest.md`'s compression-researcher entry needs its `VALID: limited | scope: research-only, no downstream impact` framing revisited in a fresh session — "research-only" was the ground for tolerating the known context-blindness, and it may still hold, but the entry now understates the component's state.

  *Reproducers:* `grep -c "Request failed" research/suggestions/*.md | grep -v ":0" | wc -l` returns 104; `grep -l "Request failed" research/suggestions/*.md | xargs stat -f "%z" | sort -n | uniq -c` shows 82 files at 219 bytes and 22 at 411 *(reproducer corrected September 19, second session: the original ended in `| head` and counted every file in the folder rather than the error files. After that session's live test replaced 2026-09-19.md with real output this reads 81 and 22, the 104 above reads 103, and the `diff` reproducer that follows no longer holds; see the next entry)*; `diff research/suggestions/2026-09-18.md research/suggestions/2026-09-19.md` returns date and timestamp lines only; `grep -E "arXiv hits|fetch failures" research/daily-digest/cron.log | tail -20` shows the failure-count trend and the three zero-arXiv days; `head -3 research/daily-digest/2026-09-14.md` shows the partial-failure WARNING banner.

- **September 19, 2026 (second session) — compression-researcher and research-sweep fixed same day under Founder override of Pattern D; root cause is host sleep, not the API; `WakeForJob` is not a launchd key; three corrections to the first-session entry above.** The first session's handoff said "Do not fix compression-researcher in the session that reads its script for the first time." This session read the script for the first time and fixed it. The conflict was flagged before any reading began; Founder decision, verbatim: "lets fix today." This is the third session of this shape (July 11, July 12, today); all three passed their gates; three is still not precedent. A cold fresh-session re-read of both patches is queued as insurance, as in July.

  **Findings, compression-researcher (all Tier 1 unless marked).**
  1. *Scale and start date.* First error file is 2026-04-20; the component worked every night April 1-19. 105 junk files, not 104: 82 read-timeouts (219 bytes), 22 DNS failures (411 bytes, `NameResolutionError`, all 22 identical after date/time stripping), and **one `API Error 529 Overloaded` (2026-05-14, 246 bytes) that the first session's `"Request failed"` search could not see** because it goes through a different return path. Same defect the September 15 entry named: the pattern was written against the wording last seen, not the thing described. 28 real outputs exist in total; **the last real output before today was 2026-06-08.**
  2. *One flaw, four paths.* `call_claude` returned error text as its return value on all four failure paths (no key, non-200, exception, and by extension any non-research 200); `main` wrote whatever came back to `OUTPUT_FILE` and fell off the end, exit 0. The wrapper's retry logic is sound and was never reached.
  3. *The "300s API timeout" is not the API being slow.* Timeout files carry "Generated at" stamps scattered across the day (76 distinct times, mostly 10:00-16:00), not 02:05. `research/suggestions/cron.log` shows each run starting 02:01-02:15 and stamping SUCCESS hours later (Sep 19: 02:09 -> 09:43; Sep 18: 02:13 -> 11:38). One run started **Aug 11 02:09 and stamped SUCCESS Sep 8 14:34** — the process sat suspended for four weeks, which is the "uninvestigated Aug 11 - Sept 8 silence." Awake, the same call completes in ~35 seconds (two wrapper runs, 12:12 and 12:19 today). The job starts during a brief night-time wake, the host sleeps mid-request, the process resumes when the lid opens, finds a dead socket, and the read timeout fires. DNS-failure files are stamped 02:01 — network not yet up at wake.
  4. *`WakeForJob` does not exist.* `man launchd.plist | grep -c -i WakeForJob` returns 0; `pmset -g sched` shows no scheduled wake near any job time. `build_log.md` sections 2.2.1 and 4.2 describe `WakeForJob` as ensuring the schedule fires regardless of sleep state; it does nothing. Jobs run when the host happens to be awake or when it next wakes. Only the compression-researcher plist was inspected this session; the other plists are presumed, not verified, to carry the same key. `pmset -g` shows `sleep 1`, `powernap 1`. **Recorded, not fixed.**
  5. *Undocumented change in the April 19 hard reset.* `git show d86f0ff` on this script shows exactly one change, `timeout=180` -> `timeout=300`, not mentioned in the commit message. Not causal (a longer timeout cannot break a call that completed inside the shorter one). Second instance of a change riding unlabelled inside an April 18-19 commit (first: `6457e02`, text-swarm).
  6. *Ruled out by direct evidence:* model identifier (`claude-sonnet-4-6` works), the `NotOpenSSLWarning` (present on successful runs), prompt size (the launchd run is context-blind, so its prompt is smaller, and wrapper runs from `/tmp` under the same blindness succeeded).

  **Not established.** Why nightly runs succeeded April 1-19 and rarely after (host power habits are the candidate; nothing in the code explains it). What wakes the host at ~02:05 (Power Nap inferred from `powernap 1`, not verified). Whether a `caffeinate -i` assertion holds the host through that kind of wake. An early Systems Engine version of the mechanism ("idle sleep after 1 minute mid-call") was contradicted by the 35-second measurement and withdrawn in-session.

  **Fix, compression-researcher.** Commit `2832d02`; hash `4a633e94...` -> `8d19bcc2...`. All four failure paths print the reason and exit 1; nothing is written to the output path on failure. A response with no `SUGGESTION` or under 1,000 characters exits 2 — written against the thing (does this look like research: every real file has exactly 3, every junk file 0), not against the three error strings seen so far. A `caffeinate -i -w <pid>` assertion is started at the top of `main`. Verified: re-grep for error-text returns clean; no-key run exits 1 and leaves the existing file untouched; direct live run exit 0, 7,357 bytes, 3 suggestions; two production-wrapper runs from `/tmp` (launchd-like, context-blind) both `SUCCESS on attempt 1`. Note Fix A probably rescues sleep-killed nights without the assertion: the resumed process now exits 1, and the wrapper's retry five minutes later runs on an awake host.

  **Findings and fix, research-sweep.** Same suspension pattern (starts ~04:35, SUCCESS one to two hours later; awake, the whole sweep takes ~10 seconds). A read-only probe of the same 19 fetches returned 18 ok: the Andrew Ng feed is a permanent `404`; the Lex Fridman feed is 17 MB against a 10-second timeout. No arXiv rate-limiting observed at 13 back-to-back requests. **On September 14 and 15 every arXiv fetch failed while GitHub was 4/4** (derived from the log line, not observed per-fetch: 14/19 failures with 8 GitHub updates and 1 RSS item leaves 13 arXiv + 1 RSS); cause **not established and no longer recoverable**, because the script counted failures and discarded status codes. The arXiv section printed "No new results found." on those days — the sentence it prints on a genuinely quiet day. Commit `36a6fc5`; hash `ae81b844...` -> `dcba5e13...`. R1: every failed fetch recorded as (source, label, reason) in log and digest banner; an RSS feed answering 200 with no items counts as a failure (learned from a community-made substitute feed that answers 200 with zero items). R2: a fully-failed arXiv or GitHub section says FETCH FAILED, not quiet. R3: a dark arXiv or GitHub source writes the digest, then exits 1 so the wrapper retries; RSS excluded. **R3 reverses the July 12 decision that partial failure exits 0 — Founder-approved.** Watch list: Andrew Ng feed commented out with a dated note (no working feed found); RSS timeout 10s -> 60s. Verified: re-grep shows the only direct `FETCH_STATS` increments are inside the two helpers; live run 29/8/1 with `0/18` failures; simulated arXiv outage on the host named all 13 failures, printed SOURCE DARK, exited 1, and wrote both the banner and the FETCH FAILED section; real digest restored after.

  **Corrections to the first-session entry above.** (a) Its size reproducer ended in `| head` and counted every file in the folder, not the error files; "82 + 22 = 104" were two unrelated counts that happened to sum. Corrected in place today; the numbers turned out right, the evidence for them was not. Fifth truncated-view instance in two days and the first embedded in canon. (b) "At least three months" is five. (c) This session's live test replaced `2026-09-19.md` — the file that entry quotes — with real output, so its `diff 2026-09-18 2026-09-19` reproducer no longer holds and its count now reads 103. The Systems Engine flagged this after the run, not before. `2026-09-18.md` still shows the shape. A sixth truncated-view instance occurred in this session (Systems Engine read a `git status -sb | head -5` list as complete; corrected next turn).

  **Surfaced by the first un-truncated `git status`, not investigated.** Six untracked mode1-loader ERROR sidecars (July 15, 17, 18, 22, 23, 27) with no ledger record; several dates coincide with DNS-failure nights. No agent output has been committed since about July 12 — every digest and suggestion file since then, including the files this ledger's reproducers depend on, exists on one laptop. *Update September 19, third session:* both items closed the same day. The six sidecars are identified in the third-session entry below. All 85 laptop-only files (35 digests, 38 suggestion files, the six sidecars, and six modified log and calibration data files) were committed and pushed as `0eee1f8`, after a secret scan across all 85 returned 0. Junk files were included on purpose, as evidence. *Reproducer:* `git show --name-only --format= 0eee1f8 | grep -c .` returns 85.

  **Open, deliberately untouched.** `state_manifest.md`'s compression-researcher entry: its line-number citations are all stale (the patch added 23 lines) and its VALID framing awaits its own decision. The relative-path context-blindness (lines 14/18 as of April 29) is unaddressed — tonight's nightly output will be real but still context-blind. The system prompt still says "Mac Mini (arriving April 9-16)". 105 junk files remain in `research/suggestions/` — evidence now, and a reload precondition for commercialization-agent, which reads that folder. The component re-suggested "Efference Copy Compression" today, its April 1 first suggestion; it has no memory of its own output. **Class-level, unsolved:** both components now fail loudly into `cron.log`, which nobody reads — the same place research-sweep's correct July banner went to die. Getting failure in front of the Founder is the actual fix for the class and needs its own decision.

  **Pattern queue.** Unchanged at four, not minted here (discovery session). Input for whoever mints silent-success: all three instances share one root — a failure converted into a *return value* and handled as data (kalshi-pull's swallowed exception, research-sweep's counted-and-discarded fetch errors, compression-researcher's error string returned as research). The `WakeForJob` finding may belong with the engine-prescribed-change marker (Pattern F candidate): a key that does nothing, present since the April 4 launchd migration per `build_log.md`, described there as working in `build_log.md`.

  **Tomorrow's check (decides whether the wake assertion works).** `grep -E "Attempt [0-9] of 3|SUCCESS on|FAILED on" research/suggestions/cron.log | tail -6`. `SUCCESS on attempt 1` near 02:10 = the assertion held. `FAILED on attempt 1` then `SUCCESS on attempt 2` in the morning = it did not, and Fix A rescued the night. If it held, the right home for it is the shared wrapper, once, not each script.

  *Reproducers:* `grep -l "Request failed" research/suggestions/*.md | xargs stat -f "%z %N"` lists every request-failure file with size, first line 2026-04-20; `cat research/suggestions/2026-05-14.md` shows the 529; `grep -L "Request failed" research/suggestions/*.md | xargs grep -c "SUGGESTION"` shows 3 for every real file and 0 for 05-14; `grep -l "Read timed out" research/suggestions/*.md | xargs grep -h "Generated at" | sort | uniq -c` shows the scattered stamps; `grep -E "Attempt 1 of 3|SUCCESS on" research/suggestions/cron.log | tail -24` shows the hours-long gaps and the Aug 11 -> Sep 8 pair; `man launchd.plist | grep -c -i WakeForJob` returns 0; `git show d86f0ff -- experiments/week1/scripts/latent_compression_researcher.py` shows the single timeout change; `shasum -a 256 experiments/week1/scripts/latent_compression_researcher.py` returns `8d19bcc2...`; `shasum -a 256 experiments/week1/scripts/research_sweep.py` returns `dcba5e13...`; `grep -n -E 'return f?"(ERROR|API Error|Request failed)' experiments/week1/scripts/latent_compression_researcher.py` returns nothing; `git show --stat 2832d02 36a6fc5`.

- **September 19, 2026 (third session) — cold re-read of `2832d02` and `36a6fc5`: 3 of 3 PASS, no defects; six July mode1-loader ERROR sidecars identified (DNS failures, four local days not six); host-sleep suspension confirmed in the benchmark loader at Tier 1; one daily loader output overwritten through UTC-date file naming.** Fresh bundle-initialized session (five files plus the 2026-09-19b handoff), HEAD `f24ec4c` at start. This session did not write the patches it reviewed. Read-only throughout: no script, registry, or manifest entry was changed.

  **Cold re-read (insurance owed on the second-session Pattern D override — now cashed).** Both commits read in full from `git show` (242 lines; completeness verified by checking every hunk's line count against its `@@` header, all thirteen match). (1) *SystemExit through `except Exception` — PASS.* `fail()` raises `SystemExit`, which is not an `Exception` subclass; all three call sites (before the `try`, inside it, inside the handler) propagate. The bottom-of-file call path is outside the diff; it is covered by the second session's host no-key test (exit 1), not by this reading. (2) *Could a legitimate response fail the threshold — PASS.* 28 of 28 real files (second-session figures) carry three `SUGGESTION`s at 6,000-7,000 bytes against a bar of one and 1,000 characters. (3) *Does a dark retry leave a truthful digest — PASS.* Whole-file search of write and exit sites in `research_sweep.py`: total-failure refusal at lines 160-161 precedes the single write at 210-211 (mode `"w"`, replace), source-dark exit at 220-221 follows it. Every retry combination leaves a truthful file; a good attempt exits 0 so the wrapper cannot retry over it. All three fetchers traced: no fetch can be counted as two failures, so `failures == attempts` is reachable. *Not seen:* the `if` condition at line 159, inferred from the message beneath it.

  **Observations, not defects, no fixes proposed.** (a) The `SUGGESTION` check is uppercase-only; a legitimate "Suggestion 1" response would exit 2 — safe direction, but the rejected text is discarded, only 200 characters reach the log. (b) The bar is one `SUGGESTION`; the evidence is three in every real file. (c) RSS treats HTTP 200 with no items as a failure; arXiv and GitHub do not. If arXiv answers 200 with empty results on every query the digest prints the quiet-day sentence and exits 0. Whether that can occur on a healthy day was not established. (d) A 200 with an unexpected body shape is logged as "Request failed" — loud, exit 1, mislabelled. (e) Failure reasons record exception type only, which keeps URLs and anything sensitive out of log and digest. (f) A manual same-day re-run that goes source-dark replaces an earlier good digest with a FETCH FAILED one — truthful, but the good results are lost. *Also noted by reading:* this ledger records a cold re-read for the July 11 override and now for today's; none is recorded for the July 12 research-sweep fix.

  **The six sidecars.** All six (241 bytes each) are `error_kind: fetch`, `URLError [Errno 8] nodename nor servname provided` after the loader's three internal attempts — no network. The loader behaved as designed: failed loud, wrote a sidecar, preserved the last good file. **Sidecars and daily outputs are named by UTC date, and `loader_run_at` is UTC.** Converted, the six files are four local days: July 15, 17, 22, 27. The files dated 07-18 and 07-23 are the wrapper's *attempt 2* for July 17 and July 22, run that evening and filed under the next UTC day. Three of the four days (17, 22, 27) have no research digest. The seventh file matched by the search, 07-11, is the tracked identity-error sidecar from the July 11 install entry (3 of 8 returned, five missing ids) — confirmed by content.

  **`experiments/benchmark/mode1/cron.log`, read in full (158 lines).** (1) *The wrapper's 300-second retry wait is suspended by host sleep.* July 17: FAILED 08:33 CDT, attempt 2 at 20:06 PDT — about 13.5 hours. July 22: FAILED 06:47, attempt 2 at 19:13. **July 27: FAILED 08:02, attempt 2 on July 31 at 19:55 — four days.** (2) *Single attempts are suspended mid-run:* work whose retry waits total 40 seconds spans 56 minutes (July 15), 1h42 (July 17), 1h51 (July 22), 3h02 (July 27). This extends the second-session host-sleep finding from the two research components to the benchmark loader, from the loader's own log. (3) *Gaps in the daily series:* no loader run on the local mornings of July 28-31; **no log lines at all between Aug 11 04:56 and Sep 8 14:33** — the same four-week window as the research components, resuming one minute before compression-researcher's Sep 8 14:34 stamp. `state_manifest.md`'s mode1-loader entry records neither gap. (4) *One daily output overwritten.* The July 31 19:55 PDT run wrote `market_state_2026-08-01.json` (live=2 retired=6); the Aug 1 04:53 scheduled run wrote the same path (live=1 retired=7). Same shape on July 18 (04:07 attempt 3, then 04:50 scheduled run; identical counts, no loss). Daily outputs are gitignored, so there is no git copy; the log line is what survives. Established from the log's write lines; the file's current content was not inspected. (5) The loader's internal retry rescued four mornings where the first fetch hit DNS failure (Jul 16, Aug 9, Sep 13, Sep 16). (6) Start times drift 04:50-05:06, consistent with the second-session finding that nothing schedules a wake.

  **Not established.** Whether a suspended wrapper also blocks launchd from starting the next morning's run, or the host was simply asleep July 28-31 — the log cannot separate them. Why the log's zone label reads CDT from the July 15 retry through the morning of July 17 (Founder does not recall; not established). Whether the loader is affected on nights that left no trace: a host that never wakes writes no sidecar, so absence of sidecars after July 27 is not evidence of health.

  **Corrections to this session's own statements.** The Systems Engine first read six sidecars as six failed days (file names are UTC dates); first reported the July 17 retry gap as 11.5 hours by comparing a CDT stamp to a PDT stamp without converting (13.5 is correct); and wrote a search pattern wider than its target (seven files for six). All three caught in-session, the second at drafting time. No truncated-view instance this session; the defences used were count-then-clipboard with the count checked on arrival, and hunk-header arithmetic on the diff.

  **Status.** Recorded, nothing fixed. The loader findings belong with the `WakeForJob` work, which waits on tomorrow morning's `research/suggestions/cron.log` check. Manifest untouched, including the compression-researcher entry. Pattern queue unchanged at four; nothing minted.

  *Reproducers:* `git show 2832d02 36a6fc5 | wc -l` returns 242; `grep -n -E 'open\(|write|OUTPUT_FILE|exit|SystemExit|def main' experiments/week1/scripts/research_sweep.py` shows the refusal (160-161), the write (210-211), the dark exit (220-221); `tail -n +1 experiments/benchmark/mode1/market_state_ERROR_2026-07-*.json` prints all seven sidecars whole; `wc -l experiments/benchmark/mode1/cron.log` returns at least 158; `grep -n -E "Retrying in|Attempt [23] of 3" experiments/benchmark/mode1/cron.log` shows each retry gap; `grep -n "market_state_2026-08-01" experiments/benchmark/mode1/cron.log` returns two write lines; `grep -n -E "Aug 11|Sep  8" experiments/benchmark/mode1/cron.log` shows the two sides of the four-week gap; `grep -n "cold re-read" docs/incident_ledger.md` for the July 12 absence.

- **September 19, 2026 (fourth session) — two open research-sweep items settled at source; July 12 fix `755a79c` cold re-read 3 of 3 PASS; 17 template digests on disk, not 6; `COMPETITIVE_WATCH` is never fetched and the digest's Competitive Watch section is static text; kalshi-pull empty-answer ticket fixed under Founder approval (`d7c8d09`); kalshi-pull has only ever saved the first page; enumeration clause ratified.** Fresh bundle-initialized session (five files plus the 2026-09-19c handoff), HEAD at start one commit past `d76273d`. Read-only for the first four jobs; one code change (kalshi-pull), made after a Context Declaration. This session did not write `755a79c`, which it re-read.

  **research-sweep, read at source (hash `dcba5e13...` verified at read time).** (1) The condition at line 159, inferred by the third session from the message beneath it, was read directly: `if FETCH_STATS["attempts"] > 0 and FETCH_STATS["failures"] == FETCH_STATS["attempts"]:`. The third session's PASS on check 3 now rests on direct evidence. (2) `arxiv_search` (lines 86-107) records a failure only on a non-200 status (line 103) or an exception (line 105). A 200 answer with no entries yields zero results and zero failures, `source_dark` stays false, the digest prints the quiet-day sentence, exit 0. Third-session observation (c) is confirmed at source; the same gap exists for GitHub. *Not observed in practice:* of 130 logged runs, 27 found zero arXiv papers; the four that carry a failure tally are all at 13 or more failures of 19 (Jul 31 14/19, Sep 13 17/19, Sep 14 14/19, Sep 15 14/19), consistent with arXiv fully dark; the other 23 predate the tally. No run with a working arXiv has found zero. On this host zero arXiv hits has never meant a quiet day. Recorded as "not observed in four measured runs", not "cannot happen". *Observations, no fixes proposed:* line 97 matches ids beginning `http://arxiv.org/abs/` only, so an `https` id would yield zero results with zero failures on every term (Pattern G shape); line 91 sends multi-word terms unquoted, consistent with the May 2 broad-matching finding; the `attempts > 0` half of line 159 stays quiet if nothing is attempted.

  **17 template digests, not 6 (Tier 1 from two independent sources).** The log holds 17 runs with zero results from every source; the digest folder holds 17 files of exactly 1,711 bytes; the dates are the same 17, and the folder search was not given the dates. They are 2026-04-25, -26, -27; 05-05, -06, -28, -31; 06-05, -09, -11, -16, -17, -18, -19, -23, -26; 07-11. The July 12 entry and the manifest named the last six. Eleven are new, the earliest April 25, so the silent-success defect was writing false "all quiet" digests about seven weeks before the first date on record. All eleven are Attempt 1; before the July 12 fix such a run exited 0, so no retry followed and nothing replaced the file. May 5 and 6 are the two no-network mornings of the May 9 kalshi-pull finding. This is the Pattern B shape: the July 12 count tracked the window examined (19 recent runs), not the folder. *Separately,* six pre-fix runs found GitHub updates and zero arXiv papers (Apr 13, May 14, May 17, May 26, Jun 3, and one undated run that predates the wrapper stamps); with no failure tally the cause is unrecoverable, and their arXiv sections print the quiet-day sentence. *After the fix,* July 31 is a fourth arXiv-dark day (14/19) beside September 13-15; `2026-07-31.md` is 3,483 bytes, the same size as `2026-09-14.md` and `2026-09-15.md`. That run is an Attempt 2 stamped Jul 31 19:55 PDT, the same minute the mode1 loader's suspended retry resumed: a second log for the host-sleep finding. It also shows research-sweep names its output by local date (one instance), where the loader uses UTC. Two same-size pairs of real digests (Apr 4-5, Apr 18-19) are weekends, consistent with arXiv publishing nothing new; sizes only, content not compared.

  **Cold re-read of `755a79c` (the July 12 fix; the ledger recorded none until now): 3 of 3 PASS, no defects.** The script diff and commit message were read whole: 125 numbered lines, all six hunk headers match their line counts, and the hunks total 25 added and 7 removed, the 32 git reports. (1) All three `except: pass` blocks are replaced and non-200 answers are counted. (2) No fetch can be counted twice: the non-200 check is the last statement inside each `try`. (3) The total-failure refusal precedes any digest text and nothing encloses it. A whole-file search then showed exactly three `requests.get` calls (lines 93, 115, 131), three `except` blocks, and two exits (161, 221): there is no fourth fetch path. *Safety note:* the full commit matched the secret pattern once; per-part counts placed the match in the committed digest (the word "token" in a paper title), so only the message and the script diff were displayed.

  **`COMPETITIVE_WATCH` is defined and never used; the digest's Competitive Watch section is static text.** The name appears once in the file (line 59). `git log -S` returns one commit, `56f63e9` "research: updated sweep with X watchlist + Aaru competitive monitoring": the list was added and never fetched, and the commit title claims more than the change did. The digest's `## Competitive Watch` section (lines 24-28) is byte-identical on 2026-04-04 and 2026-09-19: three fixed lines naming Aaru, VectorArc/AVP, CulturePulse and Simile, printed daily under a heading that reads as a finding. `intent.md` lists competitive watch as part of this component's purpose; this is the intent-versus-live-state shape of the Precedence Rule and was flagged to the Founder, not resolved. Competitor repos and terms in the ordinary GitHub and arXiv lists were not examined, so "no competitive watch happens" is not established. *Founder recollection (Tier 3):* early in the project he read competitor findings daily; which source that was (this static section, the two strategy agents unloaded April 19, or the GitHub section) is not established. The Founder asked for a scan of who else works in the area; the Systems Engine ran six live web searches and filed the result as Tier 3 at `founder_inputs/2026-09-19_competitive_scan.md`. Not canon. The Founder then proposed building nightly competitor collection in-session. The Systems Engine advised against: wiring the existing list would be a third instance of the matching-without-a-contract failure (Section 7 Finding 2; May 2 audit); it is a feature, not a fix, and nothing is failing; and it would confound the first scheduled night of `36a6fc5`. The Founder chose requirements first. Nothing was built.

  **kalshi-pull: the May 9 empty-data ticket, closed.** Context Declaration first; correctness depended on the unread script, so it was read before any option was offered. The safety count returned 2 and nothing was shown; a masked view then established that both matches (lines 28-29) carry no key-shaped literal: they reference an API key from the environment and a Bearer header, left over from before the April 29 deletion. Harmless, untouched. *Findings at source (hash `8fc24ac8...`, 58 lines):* (a) old line 38 converted any unrecognised answer into an empty list, so a schema change, not only an empty answer, would have written `[]` and reported SUCCESS every morning; wider than the ticket; (b) the write preceded any check, so a bad retry could overwrite a good same-day file; (c) the script requests `limit=1000` and ignores the `cursor`. **All 124 saved files hold exactly 1,000 markets, and a live probe returned 1,000 markets with a non-empty cursor: the feed has been the first page only since its first pull.** Truthful and incomplete; nothing loaded reads it. This is the Pattern G Form 1 shape in a second API; it is same-day evidence, recorded and not counted. The empty-file failure itself has never occurred (0 of 124). *Fix, commit `d7c8d09`, hash `8fc24ac8...` -> `2549a452...`, 12 added 1 removed:* an unrecognised shape exits 2; markets empty or not a list exits 2; both precede the write; exit 1 still means Kalshi could not be reached; a log NOTE states first-page-only whenever a cursor is present. `sys.exit` inside the `try` is not caught by `except Exception`. *Verified on host:* an offline harness (now `experiments/week1/scripts/test_kalshi_pull.py`) feeding nine fake answers reported 3 of 9 before and 9 of 9 after, each bad answer exiting 2 and leaving an existing good file byte-identical; one live run exit 0, 1,000 saved, NOTE printed. *Not verified:* a run through the launchd wrapper. The patch was applied by an all-or-nothing script that checked the file hash first and was tested on spaces-indented and tabs-indented stand-ins, because lines 28-29 were never seen. **Fourth same-session fix (July 11, July 12, September 19 second session, this one). All four passed their gates; four is still not precedent. A cold re-read of `d7c8d09` is owed.** Pagination was deliberately not changed: whether the feed should be all pages, a relevant subset, or nothing is a purpose question and a Founder decision, as is whether `VALID: yes` should carry a scope. One clue for that decision: the first market on the live page was a multi-leg NFL parlay; the share of such markets in the first page was not measured. *Left alone on purpose:* Pattern H's "Also open" sentence about kalshi-pull is now out of date. It was not edited, because Pattern H is closed to this session until its cold re-read; that re-read should correct it.

  **Enumeration clause ratified.** The enumeration defence, offered September 14 and 15 and never decided, was put to the Founder as three options: a standalone protocol, a clause inside the Reproducer requirement, or no written form. Founder chose the clause, and approved the wording. It is decided on the September 14-15 evidence alone. Two same-day instances are recorded and not counted: the 6-versus-17 digest count above, and `COMPETITIVE_WATCH`, found only because the search ran over the whole file for the thing itself. The pattern-minting queue is now empty. The deferred markers (Pattern F candidate, Pro-Thesis Optimization Loop candidate) are unchanged. The third session's unconcluded idea that Patterns C, G, H and the enumeration defence share a root ("a label is not the thing it names") gained three more same-day instances (a section heading, a commit title, a count of 1,000 that read as a total); still noted, not concluded.

  **mode1-loader manifest entry updated, facts only.** The third session's reproducers were rerun first so this session saw the evidence itself; all matched: retry waits of 13h33 (Jul 17, across a CDT-to-PDT label change), 12h26 (Jul 22) and 4d 11h53 (Jul 27 to Jul 31), against 5m01s on Jul 15 when the host stayed awake; both writes of `market_state_2026-08-01.json`; and both series gaps shown by adjacent log lines (83/84 and 120/121). *Clue, not an answer, on the third session's open CDT question:* on July 15 the stamp label changes from PDT to CDT inside that five-minute awake window, and research-sweep's log carries two IST stamps (Jun 3, Jun 5). That points at the host's time-zone setting changing, not at a logging fault. Whether the Founder was travelling on those dates is his to confirm.

  **Method.** Truncated-view instances: zero. Defences used: a secret-count gate before any file or diff was displayed (it fired twice, correctly); numbered output plus hunk arithmetic to prove a diff whole; tallies checked against their own totals (130, 27, 17); whole-folder searches that were not given the expected answer. *Systems Engine errors, all caught in-session:* twice asked the Founder to paste from the clipboard after `pbcopy`, which cannot work with his habit of copying terminal output (the copy overwrites the clipboard); replaced with numbered terminal output. Wrote messages too dense for the Founder's stated plain-language preference and was asked three times to re-explain; the preference has to lead every message, not follow a request. In the harness, predicted five failing cases where there were six, left a `__pycache__` folder, and misreported a line count by one; all three caught by testing on a stand-in before delivery.

  **Status.** One code commit (`d7c8d09`). Manifest: HEAD anchor, enumeration clause, and card additions for mode1-loader, research-sweep and kalshi-pull; no VALID label touched. Owed: cold re-read of `d7c8d09`; Pattern H cold re-read September 21 or later (unchanged). Open Founder decisions are listed in `founder_inputs/2026-09-19d_end_of_session_handoff.md`.

  *Reproducers:* `shasum -a 256 experiments/week1/scripts/research_sweep.py` returns `dcba5e13...`; `awk 'NR==159' experiments/week1/scripts/research_sweep.py`; `grep -c "arXiv hits" research/daily-digest/cron.log` returns at least 130; `grep -E "arXiv hits: 0( |$)" research/daily-digest/cron.log | sort | uniq -c` shows the 27 zero-hit lines by shape; `awk '/Attempt [0-9] of 3/ {s=$0} /arXiv hits: 0( |$)/ {print s " => " $0}' research/daily-digest/cron.log` dates them; `stat -f "%z %N" research/daily-digest/2026-*.md | awk '$1==1711'` lists the 17; `git show --stat 755a79c`; `grep -n -E "COMPETITIVE_WATCH|requests\.|except|SystemExit" experiments/week1/scripts/research_sweep.py`; `git log --oneline -S "COMPETITIVE_WATCH" -- experiments/week1/scripts/research_sweep.py` returns `56f63e9` only; `diff <(sed -n '24,28p' research/daily-digest/2026-04-04.md) <(sed -n '24,28p' research/daily-digest/2026-09-19.md)` prints nothing; `shasum -a 256 experiments/week1/scripts/kalshi_pull.py` returns `2549a452...`; `python3 experiments/week1/scripts/test_kalshi_pull.py` reports 9 of 9; `python3 -c 'import json,glob,os,collections; fs=glob.glob(os.path.expanduser("~/Projects/data/kalshi/markets_*.json")); print(sorted(collections.Counter(len(json.load(open(f))) for f in fs).items()))'` shows every file at 1,000; `git show --stat d7c8d09`.
